# Formspec Identity Binding Profile v1.0 (LLM Reference)

<!-- Generated by scripts/generate-spec-artifacts.mjs. Do not edit by hand. -->

Source spec: `specs/identity-binding/identity-binding-profile-spec.md`
Source schema: `schemas/identity-binding-profile.schema.json`

## Bottom Line Up Front

- Identity Binding Profile is a sidecar that tells processors how to bind signer, filer, reviewer, or agent identity evidence into existing Formspec identity surfaces.
- The sidecar is declarative profile metadata; it does not carry provider tokens, WebAuthn assertion bytes, credential private material, or a new Response wire shape.
- The SC-4 WebAuthn binding requires per-act `userVerification: "required"` and binds `clientDataJSON.challenge` to `AuthoredSignature.signedPayload.digest` through `formspec.webauthn.challenge.v1`.
- WebAuthn assertion bytes live in upstream `SignatureArtifact(kind="webauthn")` evidence, while server verdicts live in `ValidationArtifact(kind="webauthn-server-attestation")`; this sidecar references those contracts instead of forking them.

## Critical Schema Fields

| Pointer | Required | Type | Guidance | Description |
|---|---|---|---|---|
| `#/$defs/BindingProfile/properties/purpose` | yes | string | Separates signer, filer, reviewer, agent, and session identity concerns | Identity-binding use case this profile supports. |
| `#/$defs/EvidenceBindings/properties/authoredSignatureIdentityBinding` | yes | string | Binds profile output to the existing Response authored-signature identityBinding field | Response authored-signature field that records the provider-neutral binding result. |
| `#/$defs/EvidenceBindings/properties/identityClaimEvidenceRef` | yes | string | Binds adapter evidence into the normalized IdentityProvider claim instead of provider-native fields | formspec-web IdentityProvider evidence pointer that captures provider evidence by reference. |
| `#/$defs/WebAuthnAssertionBinding/properties/signatureMethod` | yes | string | Pins the SC-4 WebAuthn method URI | Signature-method registry URI for the WebAuthn/FIDO2/passkey profile. |
| `#/$defs/WebAuthnBinding/properties/origins` | yes | array | Pins the browser origin set for WebAuthn clientDataJSON verification | Allowed HTTPS origins that may request this WebAuthn assertion. |
| `#/$defs/WebAuthnBinding/properties/rpId` | yes | string | Pins the WebAuthn relying-party boundary for verifier checks | WebAuthn relying-party identifier expected in authenticator data verification. |
| `#/$defs/WebAuthnBinding/properties/userVerification` | yes | string | Requires PIN, biometric, or equivalent authenticator-local verification | SC-4 WebAuthn profiles require per-act user verification for signer identity binding. |
| `#/$defs/WebAuthnChallengeBinding/properties/domainSeparator` | yes | string | Keeps WebAuthn challenge bytes disjoint from other Formspec signature preimages | Domain separator for the WebAuthn challenge preimage. |
| `#/$defs/WebAuthnChallengeBinding/properties/signedPayloadDigestBinding` | yes | string | Binds the WebAuthn assertion to the existing Formspec signed-payload digest | Existing signed-payload digest field committed by the WebAuthn challenge. |
| `#/properties/$formspecIdentityBindingProfile` | yes | string | Document marker for the Identity Binding Profile sidecar | Identity Binding Profile specification version. MUST be '1.0'. |
| `#/properties/profileId` | yes | string | Portable profile-set identity | Stable URI identifying this profile document. |
| `#/properties/profiles` | yes | array | Declares the available identity binding contracts for the Definition | One or more identity-binding profiles available for the target Definition. |

## Behavioral Essentials

- Identity Binding Profile documents declare how identity evidence binds into existing Formspec identity surfaces without carrying provider-native tokens or cryptographic assertion bytes.
- WebAuthn profiles require per-act userVerification=required and HTTPS origin pinning.
- The WebAuthn challenge commits to AuthoredSignature.signedPayload.digest through the formspec.webauthn.challenge.v1 domain separator.
- Native WebAuthn assertion bytes live in SignatureArtifact(kind=webauthn), and server verdicts live in ValidationArtifact(kind=webauthn-server-attestation); this sidecar only references those contracts.
- duressCredentialSupport is only a FW-0048 discovery/composition hint and does not define routing, receipt, or safety-team policy.

## Conformance Essentials

- A conforming Identity Binding Profile must include $formspecIdentityBindingProfile=1.0, version, profileId, targetDefinition, and at least one profile row.
- Processors must reject webauthn profiles that omit webAuthn or set userVerification to any value other than required.
- Processors must reject non-HTTPS origins and any sidecar attempt to embed raw WebAuthn assertion bytes.
- Processors must bind WebAuthn profiles to urn:formspec:sig-method:webauthn-fido2@1, SignatureArtifact(kind=webauthn), ValidationArtifact(kind=webauthn-server-attestation), and AuthoredSignature.identityBinding.method=webauthn.
