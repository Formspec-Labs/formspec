# Formspec WYSIWYS Ceremony v1.0 (LLM Reference)

<!-- Generated by scripts/generate-spec-artifacts.mjs. Do not edit by hand. -->

Source spec: `specs/registry/wysiwys-ceremony-spec.md`
Source schema: `schemas/wysiwys-ceremony.schema.json`

## Bottom Line Up Front

- WYSIWYS Ceremony is a UI annex over existing ADR-0136 `SignatureSurface` and `DocumentArtifact` contracts.
- The sidecar references upstream surface and artifact identifiers; it does not define a new preimage, digest, rendering-service, or signature-surface shape.
- Each ceremony row requires exact-preimage display, a scroll gate, and per-field affirmative action.
- Bulk apply, single-click adopt-and-sign, and typed-name-only as the sole act are forbidden.

## Critical Schema Fields

| Pointer | Required | Type | Guidance | Description |
|---|---|---|---|---|
| `#/$defs/AffirmativeAction/properties/mode` | yes | string | Forbids bulk signature application | Each ceremony field requires its own affirmative act. |
| `#/$defs/PreimageBinding/properties/documentArtifactContract` | yes | string | Binds the reference to the upstream DocumentArtifact contract instead of a local fork | Contract identifier for the referenced document artifact. This sidecar does not define a local DocumentArtifact shape. |
| `#/$defs/PreimageBinding/properties/documentArtifactRef` | yes | string | Names the existing rendered document artifact shown during ceremony | Reference to the existing ADR-0136 DocumentArtifact artifactId or artifactRef whose rendered bytes are shown during ceremony. |
| `#/$defs/PreimageBinding/properties/hashField` | yes | string | Selects the upstream artifact hash field rather than redefining a digest object | Existing ADR-0136 DocumentArtifact hash field used as the visual preimage commitment for AuthoredSignature.documentHash. |
| `#/$defs/Requirements/properties/displayExactPreimage` | yes | boolean | Binds visual review to the existing signed-preimage contract | The signer MUST see the exact referenced DocumentArtifact whose hash is bound through AuthoredSignature.documentHash. |
| `#/$defs/ScrollGate/properties/required` | yes | boolean | Prevents signature capture before full-surface review opportunity | A WYSIWYS ceremony MUST require a scroll gate. |
| `#/$defs/SignatureSurfaceAnnex/properties/signatureSurfaceContract` | yes | string | Binds the ceremony row to the upstream SignatureSurface contract | Contract identifier for the referenced signature surface. This sidecar does not define a local SignatureSurface shape. |
| `#/$defs/SignatureSurfaceAnnex/properties/surfaceRef` | yes | string | References the upstream signature surface instead of declaring a local replacement | Reference to the existing ADR-0136 SignatureSurface.surfaceId or URI this ceremony row governs. |
| `#/properties/$formspecWysiwysCeremony` | yes | string | Document marker for the WYSIWYS Ceremony sidecar | WYSIWYS Ceremony specification version. MUST be '1.0'. |
| `#/properties/signatureSurfaces` | yes | array | Each ceremony row binds UI controls to an existing signature surface and document artifact | Ceremony annex rows for existing ADR-0136 SignatureSurface contracts. Each row binds UI requirements to a referenced surface and DocumentArtifact without redefining either contract. |

## Behavioral Essentials

- WYSIWYS Ceremony documents are UI-requirements annexes over existing ADR-0136 SignatureSurface and DocumentArtifact contracts.
- The sidecar references upstream artifact and surface identifiers and selected hash fields; it does not define a new digest, preimage, rendering-service, or signature-surface shape.
- The ceremony sidecar is closed and has no extensions payloads, preventing extension-defined local preimage or surface forks.
- Every ceremony surface requires exact-preimage display, a scroll gate, and per-field affirmative action before signature capture.
- Single-click adopt-and-sign, bulk apply, and typed-name-only as the sole act are non-conforming.

## Conformance Essentials

- A conforming WYSIWYS Ceremony must include $formspecWysiwysCeremony=1.0, version, targetDefinition, and at least one signatureSurfaces annex row.
- Each annex row must bind to ADR-0136.SignatureSurface and ADR-0136.DocumentArtifact through explicit contract identifiers.
- Processors must reject extensions payloads that attempt to define local preimage, digest, rendering-service, or signature-surface shapes.
- Processors must reject disabled scroll gates, allowed bulk apply, missing required ceremony fields, and local preimage/surface forks.
