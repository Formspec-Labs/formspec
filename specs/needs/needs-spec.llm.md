# Formspec Needs Specification v1.0 (LLM Reference)

<!-- Generated by scripts/generate-spec-artifacts.mjs. Do not edit by hand. -->

Source spec: `specs/needs/needs-spec.md`
Source schema: `schemas/needs.schema.json`

## Bottom Line Up Front

- This document defines the Needs Document -- an authored artifact that records why software should exist: plain-language Need records (Who / What / Why / Done), each grounded in evidence from the normative channel (Rulespec assertion IRIs) or the empirical channel (Observation research records), or carrying a declared `ungroundedReason` — never silently ungrounded.
- A Need is born `proposed` or `adopted`, is superseded rather than erased, and carries an integer `revision` over its statement and grounding. An AI may file a `proposed` Need; only a human may carry one to `adopted`.
- Experience Units cite Needs through `needRefs[]` (deliberately unpinned); GENERATION anchors cite them as `need:<id>@<revision>` (deliberately pinned). Human intent tracks the Need; machine provenance tracks the revision.
- A Needs Coverage checker computes which adopted Needs no artifact serves (`NEED-COVERAGE-001`) and which Units serve no Need (`NEED-COVERAGE-002`). Coverage is reportable, never blocking.
- Needs MUST NOT affect data capture, validation, or the processing model. The Needs Document is upstream of every other Formspec artifact and is cited by them, never the reverse; this BLUF is governed by `schemas/needs.schema.json`, the canonical structural contract.

## Critical Schema Fields

| Pointer | Required | Type | Guidance | Description |
|---|---|---|---|---|
| `#/$defs/AssertionGrounding/properties/ref` | yes | string | The typed ref across the Formspec/Rulespec decision boundary | IRI of the cited Rulespec assertion or artifact (e.g., urn:rkaf:workspace:<ws>/<localId>). Opaque to Formspec processors; Rulespec owns everything behind it. |
| `#/$defs/Need/properties/id` | yes | string | The citation target for needRefs and need: anchors | Stable identifier for this Need. Unique within needs[]. Referenced by Experience needRefs[].id and by need:<id>@<revision> generation anchors. Renaming is supersession, not an edit. |
| `#/$defs/Need/properties/origin` | yes | string | Discriminates human-authored, AI-filed, and migrated needs; drives the adoption floor rules | How this Need entered the document. Closed enum. Immutable for the record's life; adoption does not rewrite entry. ai-proposed requires proposedBy and files at status proposed. |
| `#/$defs/Need/properties/revision` | yes | integer | The regeneration staleness base; anchors pin this value | Content revision covering statement + grounding (incl. ungroundedReason). Bumps on any content change; MUST NOT bump on status/adoptedBy/title/journey changes. Pinned by need:<id>@<revision> anchors. |
| `#/$defs/Need/properties/status` | yes | string | Coverage partitions on adopted; superseded/withdrawn are history, never deleted | Lifecycle status. Closed enum. proposed: candidate awaiting human judgment. adopted: a commitment (requires adoptedBy). superseded: replaced by a successor carrying supersedes (terminal). withdrawn: rejected or found wrong (terminal; the record remains). |
| `#/$defs/Need/properties/ungroundedReason` | no | string | Fail-closed declared-absence; silent ungroundedness is unrepresentable | Declared absence of evidence. Closed enum: hypothesis (we intend to validate), team-consensus (held without a citable source), self-evident (evidence would be circular). Mutually exclusive with grounding. |
| `#/$defs/ObservationGrounding/properties/method` | yes | string | Empirical method; closed so coverage queries can partition by it | How the observation was made. Closed enum. |
| `#/$defs/Statement` | no | object | Plain-language requirement statement; the verifiability bar lives on done | The Who / What / Why / Done block. Plain language is normative (spec S4.1): readable by a non-technical reader, no spec vocabulary, no solution names. done MUST describe an outcome observable by the person in who. |
| `#/properties/$formspecNeeds` | yes | string | Version pin for needs document compatibility | Needs specification version. MUST be '1.0'. |
| `#/properties/needs` | yes | array | The Need records; statement plain-language and grounding-or-declared-absence are the load-bearing rules | Substantive payload. Each Need is an evidence-grounded plain-language statement of why software should exist, with lifecycle and revision. |
| `#/properties/version` | yes | string | Needs document revision identifier | Version of this Needs Document. SemVer is RECOMMENDED. |

## Behavioral Essentials

- Load and validate a Needs Document against needs.schema.json, then apply the S6 document-integrity rules; never silently repair a violation.
- Treat origin, status, ungroundedReason, role, and method as closed enums and reject unrecognized values.
- Resolve every bundle Experience needRefs[].id against the caller-paired Needs Document; when no document is paired, resolution and coverage are inapplicable and emit nothing.
- Compute the coverage predicate over the paired (Needs Document, bundle) pair: an adopted Need is served by at least one needRefs citation or one need: anchor at any revision; a Unit is justified by at least one needRefs entry.
- Report coverage findings without blocking validation, submission, generation, or any Core operation, and never write served-ness back into the Needs Document.
- Compare a need: anchor's pinned revision to the Need's current revision only to decide staleness; v1 processors record the condition and MUST NOT emit the reserved NEED-STALE-001 / NEED-ORPHAN-001 codes.

## Conformance Essentials

- A conforming Needs Document requires $formspecNeeds=1.0, version, and needs, carries exactly one of grounding (non-empty) or ungroundedReason per Need, and satisfies id uniqueness, supersession integrity, journey resolution, and origin/status agreement.
- An ai-proposed Need is filed at status proposed with proposedBy, and only a human actor may carry it to adopted; a processor MUST NOT set adopted on behalf of an ai-agent actor or rewrite origin.
- Processors MUST NOT use a Needs Document to alter data capture, validation, requiredness, relevance, calculation, or any other Core semantics, and MUST NOT block any Core operation on a NEED-* finding.
- A Needs Document carries no priority, ordering, or scheduling meaning; processors MUST NOT derive a backlog from it or extend any closed enum outside extensions.
