# Formspec Work Tracking

Formspec-level ADR, plan, spec, research, and review tracking. Code-validated 2026-05-14. Stack-level cross-reference → [`../../thoughts/adr/TODO.md`](../../thoughts/adr/TODO.md). Full task CSV → [`TODO.csv`](TODO.csv) (346 rows, includes `status` column from swarm validation 2026-05-14).

Directories: [`adr/`](adr/), [`plans/`](plans/), [`specs/`](specs/), [`research/`](research/), [`reviews/`](reviews/), [`archive/`](archive/).

**Ticket source-of-truth.** The eight marker blocks below regenerate from `tk` epics via `make todo-sync` (see [`../../scripts/TODO-SYNC.md`](../../scripts/TODO-SYNC.md)). Merge-group tickets (M1-M28), active specs/plans, immediate-extraction items, and P4 promotion candidates are tk-managed. The ADR status table, archive counts, swarm validation snapshot, and per-section scope-expansion / test-strategy commentary stay hand-edited — they're frozen analysis or editorial overlay, not action items. Underlying TODO.csv rows remain the authoritative ticket data; merge groups are the actionable shipping units derived from them.

---

## ADR status (code-validated 2026-05-14)

| ADR | Impl | Summary |
|-----|------|---------|
| 0029 | ~75% | 164 tasks extracted — definition/theme/component/response enrichment gaps |
| 0030 | ~90% | ~45 tasks extracted — changelog, mapping, XML/CSV, theme pages |
| 0031 | ~65% | ~45 tasks extracted — registry, scoped vars, instances, multi-platform |
| 0040 | ~55% | 19 tasks extracted — MCP consolidation, outputSchema, tool count |
| 0048 | ~45% | 19 tasks extracted — FEL functions, lint rules, conformance |
| 0051 | blocked | 3 crates + WASM bridge (see Spec 2026-03-24 below) |
| 0052 | blocked | theme.pages removal blocked on deprecation path decision |
| 0053 | ~40% | 27 tasks extracted — transports, annotations, assist-chat |

## Active specs

<!-- tk:start epic=fs-btjj -->
<!-- GENERATED by scripts/generate-todo.mjs from tk epic fs-btjj — edit tickets via tk, not this section -->

Active specs tracked in formspec/thoughts/specs/. Six specs in flight (status: Not started / Partial). Rust-layout-planner-and-pdf blocks ADR 0051; definition-advisories is closest to closure (Partial, 11 tasks).

**P1 — high-leverage:**

- **Spec — rust-layout-planner-and-pdf (40 tasks, blocks ADR 0051)** · `fs-xsz4` · P1

   Spec at formspec/thoughts/specs/2026-03-24-rust-layout-planner-and-pdf.md. Not started. Three Rust crates + WASM bridge + full pipeline. Heaviest spec in the backlog (~25-40h for PDF crate alone per P4 deep-dive).

   **Acceptance:** All 40 tasks in three crates (theme, plan, pdf) + WASM bridge close; ADR 0051 unblocks.

**P2 — standard:**

- **Spec — definition-advisories (11 tasks, PARTIAL)** · `fs-4f7t` · P2

   Spec at formspec/thoughts/specs/2026-03-31-definition-advisories.md. Partial. Closest spec to closure. Closes TS duplication once Rust Pass 8 lands. See M7 (P0 spec prose) and M8 (P0 Rust Pass 8 + tests + Python).

   **Acceptance:** Spec prose (3) + Rust Pass 8 (5) + MCP surface (1) + tests (2) all close.

- **Spec — assist-chat (8 tasks, package scaffold)** · `fs-xn2q` · P2

   Spec at formspec/thoughts/specs/2026-03-26-assist-chat.md. Not started. 8 tasks. Package, ChatSession, guided walkthrough, suggestions, doc extraction. SPEC-042 deduped against P1 0053-020.

   **Acceptance:** Package created, ChatSession + guided walkthrough + suggestions + doc extraction shipped.

**P3 — sustaining:**

- **Spec — references-ontology-authoring-ux (19 tasks)** · `fs-0vsu` · P3

   Spec at formspec/thoughts/specs/2026-03-26-references-ontology-authoring-ux.md. 1 PARTIAL (ContextResolver); rest greenfield. ~36-56h — heaviest active spec by task count.

   **Acceptance:** Handlers + MCP tools + Context panel + AI suggestions ship.

- **Spec — locale-translation-management (11 tasks)** · `fs-2z3a` · P3

   Spec at formspec/thoughts/specs/2026-03-26-locale-translation-management.md. Not started except SPEC-060 (DONE). Design exists; rest greenfield. ~16-24h.

   **Acceptance:** MCP tools + Studio tab + context panel + TMS integration ship.

- **Spec — formy-extension (10 tasks, BLOCKED: submodule missing)** · `fs-uqnq` · P3

   Spec at formspec/thoughts/specs/2026-03-26-formy-extension.md. BLOCKED — submodule doesn't exist at ../formy-extension/ or anywhere in monorepo. All 10 tickets unstartable until submodule created.

   **Acceptance:** formy-extension submodule created; all 10 tasks (M3 Chrome, Mode 1-3, profile vault, WebAuthn, Firefox port) close.

<!-- tk:end -->

## Active plans

<!-- tk:start epic=fs-m4c6 -->
<!-- GENERATED by scripts/generate-todo.mjs from tk epic fs-m4c6 — edit tickets via tk, not this section -->

Active plans tracked in formspec/thoughts/plans/. Three plans (all Partial): u1-u4 MCP UX fixes, USWDS adapter tech debt, layout workspace completion.

**P2 — standard:**

- **Plan — u1-u4-mcp-ux-fixes (6 tasks, PARTIAL)** · `fs-drlg` · P2

   Plan at formspec/thoughts/plans/2026-03-16-u1-u4-mcp-ux-fixes.md. 6 tasks: addWizardPage, PAGED_ROOT_NON_GROUP fix, tests. PLAN-001/002 in M3 (P0); PLAN-003/006 in M20 (P2).

   **Acceptance:** addWizardPage handler shipped; PAGED_ROOT_NON_GROUP fix lands; tests cover both.

- **Plan — uswds-adapter-tech-debt (5 tasks, PARTIAL)** · `fs-tdta` · P2

   Plan at formspec/thoughts/plans/2026-03-29-uswds-adapter-tech-debt.md. ARIA DescribedBy migration to core. PLAN-008/009 in M4 (P0); PLAN-010 in M10 (P1) for last-mile cleanup.

   **Acceptance:** ARIA DescribedBy fully migrated to bindSharedFieldEffects; 26+ per-adapter copies removed.

**P3 — sustaining:**

- **Plan — layout-workspace-completion (6 tasks, PARTIAL)** · `fs-fgkr` · P3

   Plan at formspec/thoughts/plans/2026-04-01-layout-workspace-completion.md. PLAN-016 status change (DONE — SubmitButton fully specified). PLAN-012/014/015/017 in M21 (P2 DataTable widget catalog).

   **Acceptance:** DataTable UI + SubmitButton spec/UI complete; widget catalog updated.

<!-- tk:end -->

## Immediate extraction (highest impact, lowest effort)

These items from swarm extraction complete in a single session each. Each one closes an ADR partial, a plan partial, or wires a spec feature into runtime. Effort estimates ~1-4h.

<!-- tk:start epic=fs-h7ao -->
<!-- GENERATED by scripts/generate-todo.mjs from tk epic fs-h7ao — edit tickets via tk, not this section -->

Items from the 2026-05-14 swarm extraction that complete in a single session each. ~2-4h apiece. Mix of ADR closure (0030 theme pages), plan completion (u1-u4 addWizardPage, USWDS ARIA), spec finalization (definition-advisories), and ADR feature wiring (0048 FEL functions, 0053 DOM annotations).

- **Immediate — Plan u1-u4 addWizardPage (~1h)** · `fs-2npw` · P1

   From swarm extraction 2026-05-14. ~1h. Add 'add_group' action to formspec_page in structure.ts:52-79, wire to project.addWizardPage(). Forms the kernel of P0 M3.

   **Acceptance:** add_group action wired in structure.ts:52-79; project.addWizardPage() callable.

- **Immediate — Plan uswds ARIA DescribedBy (~3h)** · `fs-6ezg` · P1

   From swarm extraction 2026-05-14. ~3h. Move aria-describedby management into bindSharedFieldEffects, remove from all 26+ adapters. P0 M4 (PLAN-008+009) is the shipping unit.

   **Acceptance:** aria-describedby management moved into bindSharedFieldEffects; 26+ per-adapter copies removed.

- **Immediate — ADR 0030 Theme pages gap (~2h, closes only remaining ~90% ADR)** · `fs-qbci` · P1

   From swarm extraction 2026-05-14. ~2h. Add pages + regions to main theme.json (exists only in theme-pdf.json). Closes the only remaining ~90% ADR. Companion to M2 (P0 Theme pages + regions + responsive).

   **Acceptance:** theme.json gains pages + regions (currently only in theme-pdf.json); ADR 0030 closes to ~95%.

- **Immediate — Spec definition-advisories Rust Pass 8 (~4h, closes TS duplication)** · `fs-qtw7` · P1

   From swarm extraction 2026-05-14. ~4h. Forms kernel of P0 M8 (Rust Pass 8 + tests + Python). W900/W901/W902 in bind_consistency.rs.

   **Acceptance:** crates/formspec-lint/src/bind_consistency.rs ships with W900/W901/W902; TS duplicate removed.

- **Immediate — ADR 0053 data-formspec-* annotations (~3h)** · `fs-sd1s` · P1

   From swarm extraction 2026-05-14. ~3h. P0 M6 is the shipping unit (0053-017 + 0053-018). Adds toolAnnotations to AdapterContext so all adapters pick it up.

   **Acceptance:** Webcomponent emits assist annotations (data-formspec-path et al) on all rendered fields; Playwright E2E asserts count = field count.

- **Immediate — Spec definition-advisories prose §3.10.3 (~1h)** · `fs-um82` · P1

   From swarm extraction 2026-05-14. ~1h. P0 M7 is the shipping unit (SPEC-089 + SPEC-090).

   **Acceptance:** §3.10.3 inserted; §3.10 retitled; docs:check passes.

- **Immediate — ADR 0048 formatNumber/formatDate FEL (~2h)** · `fs-wm71` · P1

   From swarm extraction 2026-05-14. ~2h. P0 M5 is the shipping unit (0048-012 + 0048-013).

   **Acceptance:** fel-core/src/evaluator/core.rs dispatch registers formatNumber + formatDate; tests cover one inequality.

<!-- tk:end -->

## Archive counts

| Category | Active | Archived |
|----------|--------|----------|
| ADRs | 8 | 48 |
| Plans | 3 | 51 |
| Specs | 6 | 30 |
| Research | 24 md | 13 |
| Reviews | 2 | archived/reviews/ |

---

Stack-level ADRs (0054, 0056, 0059, 0063, 0066–0093) → [`../../thoughts/adr/`](../../thoughts/adr/).

---

## Swarm validation details (2026-05-14)

All 346 tasks validated by 9 parallel formspec-scout agents. Each task's target file was checked for feature presence. Frozen snapshot — do not regenerate without re-running the scout swarm.

| Status | Count | % |
|--------|-------|---|
| ✅ DONE | 158 | 46% |
| ⚠️ PARTIAL | 27 | 8% |
| ❌ NOT_DONE | 159 | 46% |
| ❓ UNKNOWN | 2 | <1% |

**By source group:**

| Group | DONE | PARTIAL | NOT_DONE |
|-------|------|---------|----------|
| ADR 0029 (grant examples) | 59 | 3 | 12 |
| ADR 0030 (changelog/mapping) | 46 | 1 | 3 |
| ADR 0031 (screener/registry) | 20 | 5 | 14 |
| ADR 0040 (MCP tools) | 16 | 1 | 2 |
| ADR 0048 (locale) | 7 | 4 | 8 |
| ADR 0053 (assist) | 7 | 10 | 10 |
| Plans | 2 | 1 | 13 |
| Spec 2026-03-24 (Rust) | 0 | 0 | 40 |
| Spec 2026-03-26 | 1 | 4 | 43 |
| Spec 2026-03-31 (advisories) | 0 | 0 | 12 |

**Notable partial implementations:** ADR 0053 assist tools exist but are inline in `provider.ts` rather than the spec'd file layout; ADR 0048 locale engine implementation exists in `src/locale.ts` not `src/i18n/`; Spec definition-advisories spec prose and Rust Pass 8 are fully designed but unimplemented.

---

## P0 deep-dive (2026-05-14) — 24 tickets, 3 scouts

Scouts traced each P0 ticket from symptom → root domino → product impact, evaluating whether each change should be bigger (additional files, test strategy, cascade). Merge groups below are tk-managed; scope-expansion findings and test strategy are editorial overlay (stay hand-edit because they're per-group commentary).

### Merge groups (ship together)

<!-- tk:start epic=fs-5a5s -->
<!-- GENERATED by scripts/generate-todo.mjs from tk epic fs-5a5s — edit tickets via tk, not this section -->

P0 deep-dive 2026-05-14 grouped 24 individual tickets into 8 merge groups (M1-M8) + 1 standalone. Scouts traced symptom → root domino → product impact. Each merge group is a 'ship together' unit because constituent tickets touch the same file or share a prerequisite. Effort estimates 1-5h per group. See formspec/thoughts/TODO.md 'P0 deep-dive' section for ticket lists, scope-expansion findings, and test strategy per group.

- **M8 — Rust Pass 8 + tests + Python + MCP (~5h)** · `fs-4edk` · P0

   Full stack: pipeline integration + tests + Python bridge + MCP surface. Scope-expansion: SPEC-092 Rust Pass 8 should scan screener items AND shape paths (not just definition items), extending what TS buildAdvisories checks. Test: cargo nextest run -p formspec-lint — W900 fires for required+readonly; suppressed when calculate present.

   **Acceptance:** Tickets SPEC-092, SPEC-093, SPEC-096, SPEC-097, SPEC-098 close. Pipeline integration + test coverage + Python bridge + MCP surface — full stack.

- **M1 — Grant-app shape+uri convergence (~2h)** · `fs-880a` · P0

   All four tickets touch definition.json; engine fixture is canonical source. Back-syncs share a single target. Scope-expansion: fix ALL THREE copies for convergence; verify engine fixture is canonical and propagate forward. Test: tests/conformance/suite/ + tests/unit/ — no regression on info: 0 in submit mode; uri valid dataType.

   **Acceptance:** Tickets 0029-003, 0029-015, 0029-017, 0031-023 close. All three definition.json copies (example, benchmark reference, engine fixture) converge.

- **M5 — formatNumber + formatDate FEL (~2h)** · `fs-8vu5` · P0

   Fix together or neither works. Test: cargo nextest run -p fel-core — formatNumber(1234.5) returns '1,234.5'.

   **Acceptance:** Tickets 0048-012, 0048-013 close together. Same locale-formatting module, same dispatch table.

- **M7 — Spec advisories prose §3.10.3 + §3.10 retitle (~1h)** · `fs-9nnv` · P0

   Single spec edit. Test: npm run docs:check — generated artifacts pass freshness check.

   **Acceptance:** Tickets SPEC-089, SPEC-090 close. §3.10.3 inserted, §3.10 retitled in single edit.

- **M2 — Theme pages + regions + responsive (~2h)** · `fs-ffas` · P0

   Pages need regions; regions need responsive — 3 serial PRs would conflict. One commit. theme-pdf.json already has pages/regions; web theme has orphaned breakpoints. Test: cargo nextest run -p formspec-lint + npm test — lint handles pages/regions; E2E doesn't break.

   **Acceptance:** Tickets 0030-032, 0030-033, 0030-034 close together. Pages need regions; regions need responsive — ship as one commit.

- **M3 — MCP addWizardPage (~1.5h)** · `fs-mgdq` · P0

   add_group handler + registration + registry.json prerequisite. Scope: 0031-005 has zero independent value — 11 dependent tickets blocked on it. Either ship with first dependent (0031-006) or skeleton that passes schema validation. Test: packages/formspec-mcp/tests/structure.test.ts — add_group creates definition group + sets wizard mode.

   **Acceptance:** Tickets PLAN-001, PLAN-002, 0031-005 close. add_group handler + registration + registry.json entry (prerequisite for dependent P1 M13 tickets).

- **M4 — ARIA describedBy cleanup (~1h)** · `fs-pd72` · P0

   Default + USWDS adapters, same dead code pattern. One commit. Test: Playwright E2E — aria-describedby on inputs stays in sync with desc/hint changes.

   **Acceptance:** Tickets PLAN-008, PLAN-009 close. Default + USWDS adapters use bindSharedFieldEffects; manual aria-describedby sets removed.

- **P0 Standalone — 0040-013 outputSchema (~3h)** · `fs-rls6` · P0

   Independent of other P0 work. Test: packages/formspec-mcp/tests/ — every tool validates response against outputSchema.

   **Acceptance:** Bulk mechanical change across 49 MCP tools; every tool validates response against outputSchema.

- **M6 — Assist DOM annotations (~3h)** · `fs-sva6` · P0

   Share AdapterContext injection; data-formspec-* prerequisite for toolparamdescription integration. Scope-expansion: add toolAnnotations to AdapterContext (shared type) so ALL adapters (default, USWDS, Tailwind) pick up data-formspec-* + toolparamdescription without per-adapter work. Test: Playwright E2E — [data-formspec-path] count = field count.

   **Acceptance:** Tickets 0053-017, 0053-018 close. data-formspec-* + toolparamdescription emitted via AdapterContext.toolAnnotations.

<!-- tk:end -->

### Key scope-expansion findings

P0 tickets that scouts flagged as **too narrow as written:**

- **0029-003, 0029-015, 0029-017**: The `benchmarks/tasks/grant-application/reference/definition.json` has the same drift. Fix ALL THREE copies (example, benchmark reference, engine fixture) for convergence — or at minimum verify the engine fixture is canonical and propagate forward.
- **0030-032/033/034**: theme-pdf.json already has pages/regions. The web theme has orphaned breakpoints. These tickets should be merged into one commit that adds pages WITH regions WITH responsive — otherwise the intermediate state is broken (pages without regions).
- **0031-005**: Zero independent value. 11 dependent tickets blocked on it. Either ship with the first dependent ticket (0031-006) or create a skeleton that passes schema validation and is immediately extended.
- **0053-017/018**: Should add `toolAnnotations` to `AdapterContext` (a shared type) so ALL adapters (default, USWDS, Tailwind) pick up `data-formspec-*` and `toolparamdescription` without per-adapter work.
- **SPEC-092**: The Rust Pass 8 should also scan screener items and shape paths (not just definition items), extending what the current TS `buildAdvisories` checks.

### Test strategy per group

| Group | Test location | Key assertion |
|-------|--------------|---------------|
| M1 | `tests/conformance/suite/`, `tests/unit/` | No regression on `info: 0` in submit mode; `uri` is valid dataType |
| M2 | `cargo nextest run -p formspec-lint`, `npm test` | Lint handles pages/regions; E2E doesn't break |
| M3 | `packages/formspec-mcp/tests/structure.test.ts` | `add_group` creates definition group + sets wizard mode |
| M4 | Playwright E2E | `aria-describedby` on inputs stays in sync with desc/hint changes |
| M5 | `cargo nextest run -p fel-core` | `formatNumber(1234.5)` returns `"1,234.5"` |
| M6 | Playwright E2E | `[data-formspec-path]` count = field count |
| M7 | `npm run docs:check` | Generated artifacts pass freshness check |
| M8 | `cargo nextest run -p formspec-lint` | W900 fires for required+readonly; suppressed when calculate present |
| 0040-013 | `packages/formspec-mcp/tests/` | Every tool validates response against outputSchema |

---

## P1 deep-dive (2026-05-14) — 20 tickets, 3 scouts

All P1 tickets evaluated for root-cause layer, product impact, and scope expansion. 1 duplicate found (SPEC-042 = 0053-020, same deliverable extracted from ADR and spec sources).

### Merge groups

<!-- tk:start epic=fs-8umr -->
<!-- GENERATED by scripts/generate-todo.mjs from tk epic fs-8umr — edit tickets via tk, not this section -->

P1 deep-dive 2026-05-14: 20 tickets grouped into 8 merge groups M9-M16. Component exercises, ARIA last mile, extensions back-sync, fixture constraintKinds, registry entries (blocked on 0031-005/P0), assist transport, assist consent, assist-chat scaffold. SPEC-042 = 0053-020 duplicate noted. See formspec/thoughts/TODO.md 'P1 deep-dive' section.

- **M10 — ARIA describedBy last mile (~1h)** · `fs-5g1s` · P1

   Remove 3 remaining manual sets in radio-group, checkbox-group, text-input adapters. Scope note: screener.ts has 4 MORE manual aria-describedby sets outside default adapters (lines 230/255/268/284) — out of scope for this ticket but track separately. Test: Playwright E2E.

   **Acceptance:** Ticket PLAN-010 closes. radio-group + checkbox-group + text-input adapters use core path.

- **M16 — Assist-chat scaffold (~1h, dedupe SPEC-042 → 0053-020)** · `fs-5xuj` · P1

   Create formspec-assist-chat/ with interfaces + stubs. Scope: SPEC-042 is identical (ADR 0053 and Spec assist-chat both extracted same package scaffold) — mark duplicate. Test: npm run build — package builds; createChatSession returns ChatSession.

   **Acceptance:** Ticket 0053-020 closes. formspec-assist-chat/ package created with interfaces + stubs. SPEC-042 marked duplicate-of:0053-020.

- **M13 — Registry entries (~1.5h, blocked on 0031-005/P0)** · `fs-ga4b` · P1

   BLOCKED on 0031-005 (P0 M3). 6 entries on registry.json. Scope: 'retired' has zero coverage in any fixture across entire repo — schema permits it, nothing exercises it. Test gap, not just example gap. Test: cargo nextest run -p formspec-core — registry entries pass schema validation.

   **Acceptance:** Tickets 0031-006..0031-011 close. Six entries on registry.json mapping statuses across all 4 lifecycle states (incl. retired).

- **M15 — Assist consent (requestUserInteraction) (~2h)** · `fs-gjqi` · P1

   Wire requestUserInteraction into profile.apply confirm flow + tool schema annotations. Test: packages/formspec-assist/tests/ — requestUserInteraction called when confirm: true.

   **Acceptance:** Ticket 0053-013 closes. requestUserInteraction wired into profile.apply confirm flow + tool schema annotations.

- **M11 — Extensions back-sync x-budget-version + x-help-link (~1h)** · `fs-gxys` · P1

   Add missing x-budget-version + x-help-link; align benchmark reference copy too. Scope: three-way divergence between example, benchmark reference, engine fixture. Fix example AND benchmark; engine fixture is canonical — consider making it source of truth. Test: grep across 3 copies.

   **Acceptance:** Ticket 0029-020 closes. x-budget-version + x-help-link added; benchmark reference copy aligned.

- **M14 — Assist transport (InProcess + PostMessage + HTTP) (~4h)** · `fs-krx9` · P1

   Transport abstraction blocks 0053-026. Ship abstraction + at least InProcess + PostMessage concretes in one commit. Test: packages/formspec-assist/tests/ — transport registration + tool invoke round-trips.

   **Acceptance:** Tickets 0053-004, 0053-026 close together. Transport abstraction interface + InProcess + PostMessage + HTTP transport ship as one commit.

- **M12 — Fixture constraintKinds (~1h)** · `fs-m3kx` · P1

   Add cardinality + shape validation results to submission-in-progress.json. Scope: retired lifecycle status missing from ALL registries (not just grant-app); kitchen-sink fixture also lacks it — backfill for conformance coverage. Test: Python conformance — all 6 constraintKind values found in fixtures.

   **Acceptance:** Ticket 0029-067 closes. Cardinality + shape validation results in submission-in-progress.json; kitchen-sink fixture also backfilled.

- **M9 — Component exercises (~1h)** · `fs-paeb` · P1

   Stack align/wrap, TextInput inputMode/suffix, Panel left, Modal auto — all component.json. Scope: 0029-043 ticket claims 8 inputMode values but schema only supports 5 (text/email/tel/url/search) — schema analysis gap in extraction. Test: Playwright E2E + visual — Stack wraps, url keyboard on mobile, Panel left, Modal auto-opens.

   **Acceptance:** Tickets 0029-034, 0029-035, 0029-043, 0029-045, 0029-060, 0029-061 close. All touch component.json — single commit.

<!-- tk:end -->

### Key scope-expansion findings

- **0029-020**: Three-way divergence between example, benchmark reference, and engine fixture. Fix example AND benchmark. Engine fixture is canonical — consider making it the source of truth.
- **0029-067**: The `retired` lifecycle status is missing from ALL registries (not just grant-app). Kitchen-sink fixture also lacks it. Should backfill kitchen-sink for conformance coverage.
- **0031-011**: `retired` has zero coverage in any fixture across the entire repo — schema permits it, nothing exercises it. This is a test gap, not just an example gap.
- **0053-004**: The transport abstraction blocks 0053-026. Ship abstraction + at least InProcess + PostMessage concretes in one commit.
- **0053-020/SPEC-042**: Identical tickets (ADR 0053 and Spec assist-chat both extracted the same package scaffold). Mark SPEC-042 as duplicate-of:0053-020.
- **PLAN-010**: `screener.ts` has 4 more manual `aria-describedby` sets outside the default adapters (lines 230,255,268,284) — out of scope for this ticket but worth tracking separately.
- **0029-043**: Ticket claims 8 inputMode values (`text/decimal/numeric/email/url/tel/search/none`) but schema only supports 5 (`text/email/tel/url/search`). Schema analysis gap in original extraction.

### Test strategy per group

| Group | Test location | Key assertion |
|-------|--------------|---------------|
| M9 | Playwright E2E + visual | Stack wraps, url keyboard on mobile, Panel left, Modal auto-opens |
| M10 | Playwright E2E | aria-describedby stays in sync on radio-group/checkbox-group/text-input |
| M11 | `grep` across 3 copies | x-budget-version + x-help-link present in all grant-app definitions |
| M12 | Python conformance | All 6 constraintKind values found in fixtures |
| M13 | `cargo nextest run -p formspec-core` | Registry entries pass schema validation (registry_client) |
| M14 | `packages/formspec-assist/tests/` | Transport registration + tool invoke round-trips |
| M15 | `packages/formspec-assist/tests/` | `requestUserInteraction` called when `confirm: true` |
| M16 | `npm run build` | Package builds; `createChatSession` returns ChatSession |

---

## P2 deep-dive (2026-05-14) — 20 tickets, 3 scouts

### Status changes discovered
- **0031-001, 0031-002** → **WONTFIX** (superseded by spec §1.3 — screener is now a standalone document, not embedded in definition.json)
- **PLAN-016** → **DONE** (SubmitButton is fully specified in spec, schema, types, runtime across all layers)
- **0030-047, SPEC-091** → still NOT_DONE but **BLOCKED** on P0 dependencies
- **PLAN-004, PLAN-005** → still NOT_DONE but **BLOCKED** on PLAN-001 (P0)
- **PLAN-006** → still NOT_DONE but **BLOCKED** on PLAN-003 (P2, same group)

### Merge groups

<!-- tk:start epic=fs-cy6s -->
<!-- GENERATED by scripts/generate-todo.mjs from tk epic fs-cy6s — edit tickets via tk, not this section -->

P2 deep-dive 2026-05-14: 20 tickets grouped. Component E2E tests, Registry Python facade, MCP tool consolidation 49→28, PAGED_ROOT_NON_GROUP fix, DataTable widget catalog, lint mode wiring. Status changes: 0031-001/002 WONTFIX (spec §1.3 supersession), PLAN-016 DONE.

- **M19 — MCP tool consolidation 49→28 (~4h)** · `fs-0t9q` · P2

   Test: packages/formspec-mcp/tests/integration.test.ts — pre-merge and post-merge flows identical for key authoring paths.

   **Acceptance:** Ticket 0040-017 closes. 49 MCP tools merged to 28 per merge plan.

- **P2 Stale — 0030-047 wontfix candidate (blocked on M2)** · `fs-80cy` · P2

   Blocked on M2 (P0 Theme pages + regions + responsive). Close as wontfix if pages never land.

   **Acceptance:** Ticket 0030-047 closes — either lands after M2 or marks wontfix if pages never land.

- **M21 — DataTable widget catalog + SubmitButton palette (~3h)** · `fs-9o4m` · P2

   Status: PLAN-016 already DONE (SubmitButton fully specified in spec/schema/types/runtime). Test: Vitest (studio-core) + Playwright (studio E2E) — DataTable appears in group widget palette; SubmitButton adds node.

   **Acceptance:** Tickets PLAN-012, PLAN-014, PLAN-015, PLAN-017 close. DataTable in widget catalog + UI toggle + column config + SubmitButton palette entry.

- **M18 — Registry Python facade (~1h)** · `fs-fhly` · P2

   Test: python3 -c 'from formspec.registry import Registry' — import succeeds; Registry.load() round-trips through Rust.

   **Acceptance:** Ticket 0031-018 closes. src/formspec/registry.py wraps Rust bridge.

- **M20 — PAGED_ROOT_NON_GROUP fix + tests (~2h)** · `fs-gdv2` · P2

   Add themePlacedKeys set, differentiate message, add test. Test: packages/formspec-core/tests/diagnostics.test.ts — theme-placed item fires diagnostic with 'theme-placed' wording.

   **Acceptance:** Tickets PLAN-003, PLAN-006 close (PLAN-006 blocked on PLAN-003). themePlacedKeys set added; diagnostic message differentiated; test added.

- **M22 — Lint mode wiring (~1h)** · `fs-isjp` · P2

   Add W9xx to Authoring suppressed set; add W9xx no-promotion guard + test. Test: cargo nextest run -p formspec-lint — W900 suppressed in Authoring; W900 NOT promoted in strict mode.

   **Acceptance:** Tickets SPEC-094, SPEC-095 close. W9xx added to Authoring suppressed set; W9xx no-promotion guard + test.

- **M17 — Component E2E tests (~2h)** · `fs-nqrt` · P2

   Test: Playwright grant-app specs — component properties render; altContact fields in DOM; scratchPad reads/writes via FEL.

   **Acceptance:** Tickets 0029-075, 0030-046, 0031-037 close. Playwright tests for new component props, ref fragment resolution, writable instances.

- **P2 Standalone — SPEC-091 cross-ref (~10min, blocked on SPEC-089/M7)** · `fs-oyd0` · P2

   Blocked on SPEC-089 (M7). Add one-sentence cross-ref once §3.10.3 exists.

   **Acceptance:** Ticket SPEC-091 closes once §3.10.3 exists. One-sentence cross-ref.

<!-- tk:end -->

### Test strategy per group

| Group | Test location | Key assertion |
|-------|--------------|---------------|
| M17 | Playwright grant-app specs | component properties render; altContact fields in DOM; scratchPad reads/writes via FEL |
| M18 | `python3 -c "from formspec.registry import Registry"` | Import succeeds; `Registry.load()` round-trips through Rust |
| M19 | `packages/formspec-mcp/tests/integration.test.ts` | Pre-merge and post-merge flows identical for key authoring paths |
| M20 | `packages/formspec-core/tests/diagnostics.test.ts` | Theme-placed item fires diagnostic with "theme-placed" wording |
| M21 | Vitest (studio-core) + Playwright (studio E2E) | DataTable appears in group widget palette; SubmitButton adds node |
| M22 | `cargo nextest run -p formspec-lint` | W900 suppressed in Authoring; W900 NOT promoted in strict mode |

---

## P3 deep-dive (2026-05-14) — 16 tickets, 2 scouts

### Status changes discovered
- **0029-072** → **DONE** (multiChoice array values already in sample-submission.json:42-45)
- **SPEC-099** → NOOP (condition not met — Rust Pass 8 hasn't shipped via WASM)
- **SPEC-100** → FUTURE (mapping-aware check not started)
- **0048-014→017** → BIGGER THAN DESCRIBED (require new `DocumentType::Locale` + cross-doc infrastructure, not just a lint pass) — captured in M28 description.

### Merge groups

<!-- tk:start epic=fs-ykjg -->
<!-- GENERATED by scripts/generate-todo.mjs from tk epic fs-ykjg — edit tickets via tk, not this section -->

P3 deep-dive 2026-05-14: 16 tickets grouped. Per-field currency, SummaryRow instantiation, NRB=empty fixture, ValidationReport metadata, registry enrichment (blocked on 0031-005/P0), locale lint foundation. 0029-072 DONE-discovered; SPEC-099/100 NOOP. M28 (0048-014→017) is significantly larger than 1-line tickets suggest — requires DocumentType::Locale + pass_locale.rs + LintOptions expansion + FEL extraction.

- **M25 — NRB=empty fixture (~1h)** · `fs-1vze` · P3

   Create submission-university.json exercising nonRelevantBehavior empty.

   **Acceptance:** Ticket 0029-074 closes. submission-university.json exercises nonRelevantBehavior empty.

- **M24 — SummaryRow instantiation (~1h)** · `fs-65ah` · P3

   Replace built-in Summary with SummaryRow custom component.

   **Acceptance:** Ticket 0029-031 closes. Built-in Summary replaced with SummaryRow custom component.

- **P3 NOOP — SPEC-099 + SPEC-100 (condition not met / future)** · `fs-cyne` · P3

   SPEC-099: condition not met — Rust Pass 8 hasn't shipped via WASM. SPEC-100: mapping-aware check not started. Both NOOP until prerequisites materialize.

   **Acceptance:** Closed as NOOP: SPEC-099 condition not met (Rust Pass 8 hasn't shipped via WASM); SPEC-100 future (mapping-aware check not started).

- **M27 — Registry enrichment (~1h, blocked on 0031-005/P0)** · `fs-euce` · P3

   BLOCKED on 0031-005 (P0 M3).

   **Acceptance:** Tickets 0031-012..0031-016 close. deprecationNotice, specUrl/schemaUrl, examples, mappingDslVersion, extensions all on registry.json.

- **M26 — ValidationReport metadata (~30min, highest RoI in P3)** · `fs-od7k` · P3

   3-line fix wiring definition.url/version through engine. Highest-RoI ticket in this batch — fixes real traceability hole for multi-definition deployments.

   **Acceptance:** Ticket 0030-043 closes. definition.url + version wired through engine.

- **M23 — Per-field currency on requestedAmount (~1h)** · `fs-vkpn` · P3

   Add currency to requestedAmount + optional 2nd money field. Scope-expansion: consider adding 2nd money field (euPortion with EUR) for genuine mixed-currency demo.

   **Acceptance:** Ticket 0029-005 closes. currency added to requestedAmount + optional 2nd money field.

- **M28 — Locale lint foundation (~6h, bigger than tickets describe)** · `fs-x97v` · P3

   SIGNIFICANTLY LARGER than 1-line descriptions suggest. Requires: (1) DocumentType::Locale recognition + $formspecLocale marker detection in schema_validator.rs; (2) pass_locale.rs (pass 9 in pipeline); (3) LintOptions expansion (locale documents, definition documents); (4) FEL {{expression}} extraction — if spec supports arbitrary FEL not just variable refs, L301 becomes full FEL parse; (5) Python-side validate_all() integration. ~6h basic; likely more for FEL expression support.

   **Acceptance:** Tickets 0048-014..0048-017 close. New DocumentType::Locale + pass_locale.rs (pass 9) + LintOptions expansion + FEL extraction + Python validate_all() integration.

<!-- tk:end -->

### Key scope-expansion findings

- **0048-014→017** are significantly larger than their 1-line descriptions suggest. They require:
  1. Adding `DocumentType::Locale` recognition + `$formspecLocale` marker detection in schema_validator.rs
  2. A new `pass_locale.rs` module (pass 9 in the pipeline)
  3. Cross-document `LintOptions` expansion (locale documents, definition documents)
  4. FEL `{{expression}}` extraction — if spec supports arbitrary FEL, not just variable refs, L301 becomes a full FEL parse
  5. Python-side `validate_all()` integration
  - Estimated effort: ~6h for basic passes, likely more for FEL expression support
- **0029-005**: Consider adding a 2nd money field (`euPortion` with `EUR`) for genuine mixed-currency demo
- **0030-043**: 3-line fix, highest-RoI ticket in this batch (fixes real traceability hole for multi-definition deployments)

---

## P4 deep-dive (2026-05-14) — 108 tickets, 3 scouts

Group-level analysis of all speculative/future work. Group analysis stays hand-edit (snapshot of backlog shape, not action items); promotion candidates are tk-managed below.

### Key findings by group

| Group | Tickets | Current state | ~Effort | P4 correct? |
|-------|---------|--------------|---------|-------------|
| Rust theme crate | SPEC-001→008 | TS equivalent in formspec-layout; no crate | 4-6h | Yes |
| Rust plan crate | SPEC-009→019 | TS equivalent in formspec-layout; no crate | 8-12h | Yes |
| WASM bridge | SPEC-020→023 | wasm crate exists; no theme/plan/pdf flags | 3-4h | Yes |
| Rust PDF crate | SPEC-024→040 | Greenfield; ADR 0051 blocked | 25-40h | **Promote 4a to P3** |
| formy extension | SPEC-049→058 | **Submodule doesn't exist** — all 10 BLOCKED | 30-50h | P4 but add BLOCKED label |
| Locale translation | SPEC-059→069 | SPEC-060 DONE; design exists; rest greenfield | 16-24h | Yes |
| Refs-ontology | SPEC-070→088 | 1 PARTIAL (ContextResolver); rest greenfield | 36-56h | Yes (heaviest group) |
| Assist-chat | SPEC-041→048 | Chat exists in formspec-studio; LLMAdapter not in Layer 0 | 2-3wks | Yes, arguably P5 |
| Assist PARTIALs (paths) | 0053-005→012 | Code exists at wrong paths; monolith provider.ts | ~1 day | **Should be P3** |
| Assist NOT_DONEs | 0053-019→025 | Blocked on 0053-020 (P1 scaffold) | — | Yes |
| Locale naming | 0048-005→009 | Code exists with different names; path mismatches | ~hours | **Should be P3** |
| Misc | 0040-014, 0048-011, 0048-018 | PARTIAL/NOT_DONE; no demand driver | ~1wk each | Yes |

### Promotion candidates

<!-- tk:start epic=fs-z6gd -->
<!-- GENERATED by scripts/generate-todo.mjs from tk epic fs-z6gd — edit tickets via tk, not this section -->

P4 deep-dive 2026-05-14: 108 tickets in P4 backlog. Three promotion candidates flagged: SPEC-024→027 (PDF 4a — unblocks ADR 0051), 0053-005→012 (assist path fixes — code exists at wrong paths, ~1 day organizational debt), 0048-005→009 (locale naming aliases — code exists, ~30min). SPEC-049→058 (formy-extension submodule) explicitly BLOCKED — submodule doesn't exist.

- **P4→P3 promote — Locale naming aliases (0048-005..009, ~30min)** · `fs-2qh0` · P3

   resolveLocaleString exists at FormEngine.ts:690. Fallback chain, FEL interpolation, @context suffix all exist in locale.ts and field-view-model.ts. Adding aliases or renaming is ~30min.

   **Acceptance:**
     - Decision: promote 0048-005..009 (locale naming aliases) from P4 to P3.
     - Reason: resolveLocaleString exists at FormEngine.ts:690; fallback chain, FEL interpolation, @context suffix all in locale.ts and field-view-model.ts. ~30min to add aliases or rename.

- **P4→P3 promote — PDF 4a (SPEC-024..027, unblocks ADR 0051)** · `fs-8xlr` · P3

   Font metrics + measurement + pagination are independent of other Rust crate work. ADR 0051 is BLOCKED on this. PDF output is product requirement for government forms — stack already ships PDF-specific themes.

   **Acceptance:**
     - Decision: promote SPEC-024..027 (Font metrics + measurement + pagination) from P4 to P3.
     - Reason: unblocks ADR 0051; independent of other Rust crate work; PDF output is product requirement for government forms.

- **P4→P3 promote — Assist path fixes (0053-005..012, ~1 day)** · `fs-gxp0` · P3

   All PARTIALs exist but at wrong paths or inline in 969-line provider.ts. Organizational debt, not feature gaps. Extract to src/tools/, src/shim/, src/profile/, src/resolver/ is ~1 day.

   **Acceptance:**
     - Decision: promote 0053-005..012 (path fixes) from P4 to P3.
     - Reason: all PARTIALs exist but at wrong paths or inline in 969-line provider.ts — organizational debt, not feature gaps. ~1 day to extract to src/tools/, src/shim/, src/profile/, src/resolver/.

<!-- tk:end -->

### Items that need explicit BLOCKED label

**SPEC-049→058 (formy):** `formy-extension/` submodule does not exist at `../formy-extension/` or anywhere in the monorepo. All 10 tickets are unstartable until this is created. Tracked at active-specs level via the `formy-extension` spec ticket (status: BLOCKED).
