# Experience Companion Spec Implementation Plan

> **Archived 2026-05-22.** The active implementation work is reflected in
> `specs/experience/experience-spec.md`, `schemas/experience.schema.json`,
> `specs/experience/experience-spec.llm.md`, and the Experience conformance
> fixtures/tests under `tests/conformance/`. This file is retained as historical
> execution context, not an active task queue.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land a draft Formspec **Experience** companion spec — prose, schema, examples, conformance fixtures — that satisfies the §10.1 follow-on and §9 "Experience shape" promotion gate of [`../../specs/2026-05-20-formspec-semantic-layers.md`](../../specs/2026-05-20-formspec-semantic-layers.md).

**Architecture:** New sidecar document type at `specs/experience/` + `schemas/experience.schema.json`, structurally a peer of Locale / References / Ontology. Pure metadata: a Core processor MAY ignore it; Extended processors load it for generation, review, and coverage checks. Strictly additive — no Definition / Component / Validation behavior changes. Defines `actor`, `task`, `unit`, `unit.kind` registry (closed, abstract, task-oriented), typed references (`itemRefs`, `conceptRefs`, `actionRefs`), `applicability`, and a static **coverage predicate** (required visible items → at least one unit).

**Tech Stack:** Markdown (W3C-style), JSON Schema 2020-12, `npm run docs:generate` / `docs:check` pipeline (`generate-spec-artifacts.mjs`), Python pytest conformance suite under `tests/conformance/`, auto-derived TS types via `@formspec-org/types` `types:generate`.

**Sequencing (per user direction):** prose contract first → schema encodes it → examples validate against schema → conformance fixtures pin behavior. Schema does not become the place where unresolved prose questions hide ([concept §10 closing line](../../specs/2026-05-20-formspec-semantic-layers.md)).

**Citations used throughout this plan refer to the concept note** ([`../../specs/2026-05-20-formspec-semantic-layers.md`](../../specs/2026-05-20-formspec-semantic-layers.md)) unless prefixed with another spec name.

---

## File Structure

### Created

| Path | Responsibility |
|---|---|
| `specs/experience/experience-spec.md` | Canonical prose. W3C-style, BCP-14 normative language. |
| `specs/experience/experience-spec.bluf.md` | BLUF source (3–5 falsifiable bullets). Injected by `docs:generate`. |
| `specs/experience/experience-spec.llm.md` | Generated LLM artifact. **Never hand-edited.** |
| `schemas/experience.schema.json` | JSON Schema 2020-12. `$id`: `https://formspec.org/schemas/experience/1.0`. |
| `tests/conformance/schemas/test_experience_schema.py` | Schema-acceptance pytest. PASS / FAIL fixtures. |
| `tests/conformance/spec/test_experience_coverage.py` | Coverage-predicate pytest. Required-visible-items → unit coverage. |
| `tests/conformance/fixtures/experience/valid-minimal.json` | Smallest valid Experience document. |
| `tests/conformance/fixtures/experience/valid-grant-application.json` | Realistic full document; Appendix A source. |
| `tests/conformance/fixtures/experience/invalid-missing-target.json` | `targetDefinition` absent → schema reject. |
| `tests/conformance/fixtures/experience/invalid-bad-unit-kind.json` | `unit.kind` outside registry → schema reject. |
| `tests/conformance/fixtures/experience/invalid-dangling-item-ref.json` | `itemRefs[].path` not in Definition → coverage / referential reject. |
| `tests/conformance/fixtures/experience/coverage-pass.json` | Every required-visible item appears in some `unit.itemRefs`. |
| `tests/conformance/fixtures/experience/coverage-fail.json` | A required visible item is uncovered by any unit. |
| `tests/conformance/fixtures/experience/definition-base.json` | Shared Definition used by coverage fixtures (3 items, 1 required visible). |

### Modified

| Path | Why |
|---|---|
| `thoughts/specs/2026-05-20-formspec-semantic-layers.md` | Add forward link from §10.1 to the landed spec. **One-line edit.** |
| `filemap.json` | Regenerated by `npm run docs:filemap`. **Generated — never hand-edit.** |

### Explicitly NOT in scope

- Component reference additions (`unitRef`, `taskRefs`, `actionRef`, `conceptRefs`, `x-generation`) — concept §10.4, separate plan.
- Response Actions companion spec — concept §10.2, separate plan.
- Validation mapping appendix — concept §10.3, separate plan.
- Trace — concept §10.6, separate plan.
- Definition / Component / Theme / Locale / Core / Mapping changes — additive invariant; no edits.

---

## Self-Review Note

Spec coverage of the concept note in this plan: §6.2 (Experience ownership), §10.1 (Experience companion spec order), §9 Experience-shape promotion gate (actors, tasks, units, applicability, typed refs, abstract `unit.kind`, coverage expectations, seed-from-Definition), §7.1 unit.kind candidate names, §5.1 additivity, §5.4 generated-vs-authored honesty (Experience is authored — stated explicitly), §11.5 minimum authoring bundle (deferred to Response Actions plan; this spec only contributes the Experience portion). Open questions §11.1–§11.5 are referenced but not resolved here — that is correct; this is the first follow-on of six.

---

## Task 1: Scaffold spec directory + empty files

**Files:**
- Create: `specs/experience/experience-spec.md`
- Create: `specs/experience/experience-spec.bluf.md`
- Modify: none yet (filemap regen happens at end)

- [ ] **Step 1: Create the spec directory and stub files**

```bash
mkdir -p formspec/specs/experience
touch formspec/specs/experience/experience-spec.md
touch formspec/specs/experience/experience-spec.bluf.md
```

- [ ] **Step 2: Verify sibling pattern**

Run: `ls formspec/specs/experience/ formspec/specs/locale/`
Expected: both directories show `<name>-spec.md` and `<name>-spec.bluf.md` at minimum. `experience-spec.llm.md` does NOT exist yet — it is generated.

- [ ] **Step 3: Commit scaffold**

```bash
cd formspec && git add specs/experience/
git commit -m "feat(experience): scaffold spec directory

First follow-on from thoughts/specs/2026-05-20-formspec-semantic-layers.md §10.1.
Empty files; content lands in subsequent commits."
```

---

## Task 2: Write spec frontmatter, title block, BCP-14 conventions, BLUF marker

**Files:**
- Modify: `specs/experience/experience-spec.md` (write initial header)

- [ ] **Step 1: Write the spec preamble through BLUF marker**

Replace the empty file with:

```markdown
---
title: Formspec Experience Specification
version: 1.0.0-draft.1
date: 2026-05-21
status: draft
---

# Formspec Experience Specification v1.0

**Version:** 1.0.0-draft.1
**Date:** 2026-05-21
**Editors:** Formspec Working Group
**Companion to:** Formspec v1.0 — A JSON-Native Declarative Form Standard

---

## Status of This Document

This document is a **draft specification**. It is a companion to the [Formspec v1.0 core specification](../core/spec.md) and does not modify or extend the core processing model. Implementors are encouraged to experiment with this specification and provide feedback, but MUST NOT treat it as stable for production use until a 1.0.0 release is published.

This spec was promoted from the concept architecture note [`thoughts/specs/2026-05-20-formspec-semantic-layers.md`](../../thoughts/specs/2026-05-20-formspec-semantic-layers.md) (the "Experience" semantic layer). It addresses the **Experience shape** promotion gate from §9 of that note: actors, tasks, units, applicability, typed references, abstract `unit.kind`, coverage expectations, and seed-from-Definition guidance.

## Conventions and Terminology

The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "NOT RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be interpreted as described in [BCP 14][rfc2119] [RFC 2119] [RFC 8174] when, and only when, they appear in ALL CAPITALS, as shown here.

JSON syntax and data types are as defined in [RFC 8259]. URI syntax is as defined in [RFC 3986]. JSON Pointer syntax is as defined in [RFC 6901].

Terms defined in the Formspec v1.0 core specification — including *Definition*, *Item*, *Response*, *Bind*, *FEL*, and *conformant processor* — retain their core-specification meanings throughout this document unless explicitly redefined.

Additional terms:

- **Experience Document** — A JSON document conforming to this specification, identified by `$formspecExperience: "1.0"`.
- **Actor** — A role that interacts with the form (e.g., applicant, reviewer, assister).
- **Task** — A unit of user-visible work the form supports (e.g., "identify applicant", "submit application").
- **Unit** — A grouping of typed references to Definition items, concepts, and actions, organized under a single task and `unit.kind`.
- **Coverage** — A static predicate over a Definition and an Experience asserting that every required, visibly relevant Definition item is referenced by at least one Unit.
- **Coverage-aware processor** — An Extended processor that, in addition to schema validation, computes and reports coverage findings.

[rfc2119]: https://www.rfc-editor.org/rfc/rfc2119
[RFC 3986]: https://www.rfc-editor.org/rfc/rfc3986
[RFC 6901]: https://www.rfc-editor.org/rfc/rfc6901
[RFC 8174]: https://www.rfc-editor.org/rfc/rfc8174
[RFC 8259]: https://www.rfc-editor.org/rfc/rfc8259

---

## Bottom Line Up Front

<!-- bluf:start file=experience-spec.bluf.md -->
<!-- bluf:end -->

---
```

- [ ] **Step 2: Write the BLUF source file**

Write to `specs/experience/experience-spec.bluf.md`:

```markdown
- This document defines the Experience Document — an authored sidecar JSON artifact that names abstract task intent for a Formspec Definition: actors, tasks, units, applicability, and typed references to items, concepts, and actions.
- A valid Experience Document requires `$formspecExperience`, `version`, `targetDefinition`, and at least one of `actors`, `tasks`, or `units` (at minimum one populated `units` array is RECOMMENDED).
- `unit.kind` is a closed, abstract, task-oriented registry — `data-entry`, `review`, `confirmation`, `evidence-collection`, `attestation`, `error-resolution`, `assistance` — chosen so units do not become layout containers.
- Coverage is a static predicate: every Definition item that is required and not statically non-relevant MUST appear in at least one `unit.itemRefs`; Coverage-aware processors MUST report uncovered required items.
- Experience MUST NOT affect data capture, validation, or the processing model; this BLUF is governed by `schemas/experience.schema.json`, the canonical structural contract.
```

- [ ] **Step 3: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md specs/experience/experience-spec.bluf.md
git commit -m "feat(experience): preamble + BLUF

Adds frontmatter, BCP-14 boilerplate, terminology inheritance, and BLUF source.
Closed, abstract unit.kind registry; additive invariant stated."
```

---

## Task 3: Write S1 Introduction

**Files:**
- Modify: `specs/experience/experience-spec.md`

- [ ] **Step 1: Append S1 to the spec**

Append after the BLUF marker block:

```markdown
## 1. Introduction

### 1.1 Purpose and Scope

The Formspec Experience Specification defines an authored sidecar document that names **abstract task intent** for a Formspec Definition. An Experience Document groups typed references to Definition items, concepts, and actions under named **Units**, each describing what a user is trying to do — not how a renderer should draw it.

Experience exists so that generators, reviewers, and tools can:

1. Derive Component drafts from semantic source (Definition + Experience + Response Actions).
2. Detect required, visibly relevant Definition items that no Unit covers.
3. Reason about audience, applicability, and accessibility intent without consulting Component layout.
4. Index relationships for review and regeneration tooling (Trace).

This specification does NOT define:

- Concrete layout, widget choice, page composition, or visual presentation. Those belong to **Component** and **Theme**.
- Validation rules, calculation rules, requiredness, or relevance. Those belong to **Definition** (core S4).
- Submission payloads or transformation rules. Those belong to **Mapping**.
- Form-scoped action orchestration. That belongs to **Response Actions** (forthcoming companion spec).
- Durable workflow events or governed case lifecycle. Those belong to **WOS** and **Respondent Ledger**.

### 1.2 Relationship to Formspec Core

| Layer | Concern | Defined In |
|-------|---------|------------|
| Structure | What data to collect | Core S4 (Items) |
| Behavior | How data behaves | Core S4.3 (Binds), S5 (Shapes) |
| Task intent | What the user is trying to do | **this spec** |
| Presentation | How data is displayed | Core S4.2.5 (Tier 1) + Theme + Component (Tier 2) |
| Localization | Display strings | Locale |
| Boundary translation | External payload | Mapping |

Experience is **authored source**. It does not derive from Definition; tools MAY *seed* an Experience from a Definition (see S9), but the Experience-Definition relationship is not one-to-one.

### 1.3 Design Principles

1. **Additive, not invasive.** Experience MUST NOT affect data capture, validation, or the processing model. A Core processor that ignores Experience produces identical Responses.
2. **Task intent, not layout.** Units describe what the user is doing, not what the renderer draws. `unit.kind` is closed, abstract, and task-oriented (S5.2).
3. **Reference, never own.** Experience references Items by Definition path; it does not redefine Items, redefine binds, or override Component / Theme decisions.
4. **Coverage is detectable.** A static predicate (S8) MUST be expressible over (Definition, Experience) such that uncovered required visible items are identifiable without runtime evaluation.
5. **Honest provenance.** Experience documents that are seeded or regenerated MUST be authored-or-generated-or-edited; this spec does not introduce generation metadata (that lives in Component additions, concept §10.5).

### 1.4 Conformance Levels

This specification defines two conformance levels:

| Level | Requirements |
|-------|--------------|
| **Experience Core** | MUST schema-validate; MUST resolve `targetDefinition` against a loaded Definition. |
| **Experience Coverage-Aware** | All of Core, plus MUST compute and report the coverage predicate (S8) on every loaded (Definition, Experience) pair. |

A conformant Core processor MAY ignore Experience entirely. A conformant Extended processor that loads Experience MUST validate it against the schema in S11 and MUST verify `targetDefinition.url` matches the loaded Definition's `url`.

#### 1.4.1 Conformance Prohibitions

A conformant processor MUST NOT:

1. Use Experience to alter data capture, validation, requiredness, relevance, calculation, or any other Core semantics.
2. Treat Experience as authoritative for layout, widget selection, or page composition.
3. Substitute Experience for a missing Definition; an Experience without a resolvable target is invalid (S2).
4. Add `unit.kind` values outside the registry (S5.2) without using the `x-` extension mechanism (S12).
```

- [ ] **Step 2: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md
git commit -m "feat(experience): S1 Introduction

Purpose, scope, layer placement, design principles (additive, task-intent-only,
reference-never-own, coverage-detectable), two conformance levels."
```

---

## Task 4: Write S2 Document Structure (prose property table form)

**Files:**
- Modify: `specs/experience/experience-spec.md`

- [ ] **Step 1: Append S2**

```markdown
## 2. Document Structure

An Experience Document is a JSON object at the top level with the following properties. (Generated schema reference tables replace this prose table in S11 once the schema lands; the prose form here is normative until then.)

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `$formspecExperience` | string (`const: "1.0"`) | REQUIRED | Document type marker; pins to spec major version. |
| `version` | string (semver) | REQUIRED | Version of this Experience Document. |
| `targetDefinition` | object | REQUIRED | `{ url, compatibleVersions }` binding to a Definition (same shape as Theme / Component / Locale). |
| `name` | string | OPTIONAL | Machine-readable short name. |
| `title` | string | OPTIONAL | Human-readable display name. |
| `description` | string | OPTIONAL | Free-form description of audience and purpose. |
| `applicability` | object | OPTIONAL | Document-level applicability (S7). May be overridden per-Unit. |
| `actors` | array of `Actor` | OPTIONAL | Actor identities used by Units and Tasks (S3). |
| `tasks` | array of `Task` | OPTIONAL | Task identities used by Units (S4). |
| `units` | array of `Unit` | RECOMMENDED | The substantive payload (S5). An Experience with zero Units is structurally valid but trivially uncoverable (S8). |
| `extensions` | object | OPTIONAL | Extension data; keys MUST be prefixed `x-` (S12). |

At least one of `actors`, `tasks`, or `units` SHOULD be populated; a document with none of these is permitted by the schema but carries no semantic payload.

**Inline example:**

```json
{
  "$formspecExperience": "1.0",
  "version": "1.0.0",
  "targetDefinition": {
    "url": "https://example.gov/forms/intake",
    "compatibleVersions": ">=1.0.0 <2.0.0"
  },
  "title": "Intake -- applicant experience",
  "actors": [{ "id": "applicant", "title": "Applicant" }],
  "tasks": [{ "id": "identifyApplicant", "title": "Identify the applicant" }],
  "units": [
    {
      "id": "identity",
      "kind": "data-entry",
      "taskRef": "identifyApplicant",
      "actorRef": "applicant",
      "itemRefs": [
        { "path": "applicantName" },
        { "path": "dateOfBirth" }
      ]
    }
  ]
}
```
```

- [ ] **Step 2: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md
git commit -m "feat(experience): S2 Document Structure

Top-level property table (prose form, will be replaced by schema-ref marker in
Task 21) plus a minimal inline example."
```

---

## Task 5: Write S3 Actors

**Files:**
- Modify: `specs/experience/experience-spec.md`

- [ ] **Step 1: Append S3**

```markdown
## 3. Actors

An **Actor** is a named role that interacts with the form. Actors are declarative identifiers — Experience does NOT define authorization, authentication, or access control. Those are out of scope.

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | string (camelCase, unique within `actors[]`) | REQUIRED | Stable identifier. Referenced by `unit.actorRef`. |
| `title` | string | OPTIONAL | Human-readable label. |
| `description` | string | OPTIONAL | Free-form role description. |
| `extensions` | object | OPTIONAL | `x-`-prefixed extension data. |

**Common actors** (informative): `applicant`, `respondent`, `reviewer`, `approver`, `assister`, `agent`, `caseworker`. These are not enumerated by this spec; authors choose identifiers that fit their domain.

Actors are referenced by:

- `unit.actorRef` (S5.1) — the actor for whom the unit's task is intended.
- `task.actorRefs[]` (S4) — the actors who participate in a task.
- `applicability.actorRefs[]` (S7) — actor predicates for applicability resolution.

A processor MUST reject an Experience that references an `actorRef` not present in `actors[]`. (Schema enforces shape; this rule is referential and enforced by Coverage-aware processors and validators.)
```

- [ ] **Step 2: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md
git commit -m "feat(experience): S3 Actors

Actor identity shape, referential integrity rule, common actors as informative
guidance."
```

---

## Task 6: Write S4 Tasks

**Files:**
- Modify: `specs/experience/experience-spec.md`

- [ ] **Step 1: Append S4**

```markdown
## 4. Tasks

A **Task** is a named unit of user-visible work that the form supports. Tasks are abstract; they describe what the user is doing, not the steps a Component takes to render that work.

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | string (camelCase, unique within `tasks[]`) | REQUIRED | Stable identifier. Referenced by `unit.taskRefs[]`. |
| `title` | string | OPTIONAL | Human-readable label. |
| `description` | string | OPTIONAL | Free-form description. |
| `actorRefs` | array of string | OPTIONAL | Actors participating in this task. Each entry MUST resolve to an `actor.id`. |
| `extensions` | object | OPTIONAL | `x-`-prefixed extension data. |

Tasks SHOULD be named in user-domain language (e.g., `identifyHousehold`, `reviewEligibility`, `attestAccuracy`), not in renderer-domain language (`fillFormPage1`, `showSummaryCard`).

A Unit MAY reference zero, one, or many tasks via `unit.taskRefs[]` (S5.1). A Task without any referring Unit is permitted but is informationally inert — it signals planned work not yet bound to data collection.

A processor MUST reject an Experience that references a `taskRef` not present in `tasks[]`.
```

- [ ] **Step 2: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md
git commit -m "feat(experience): S4 Tasks

Task identity, user-domain naming guidance, referential integrity."
```

---

## Task 7: Write S5 Units + closed unit.kind registry

**Files:**
- Modify: `specs/experience/experience-spec.md`

- [ ] **Step 1: Append S5**

```markdown
## 5. Units

A **Unit** is the substantive payload of an Experience Document. Each Unit groups typed references to Definition items, concepts, and actions under a single task and `kind`.

### 5.1 Unit Shape

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | string (camelCase, unique within `units[]`) | REQUIRED | Stable identifier. Referenced by Component nodes via `unitRef` (forthcoming, concept §10.4). |
| `kind` | string (registry, S5.2) | REQUIRED | Abstract task-oriented classification. |
| `title` | string | OPTIONAL | Human-readable label. |
| `description` | string | OPTIONAL | Free-form description of what the user is doing in this unit. |
| `actorRef` | string | OPTIONAL | The actor this unit is intended for. MUST resolve to an `actor.id`. |
| `taskRefs` | array of string | OPTIONAL | Tasks this unit advances. Each entry MUST resolve to a `task.id`. |
| `itemRefs` | array of `ItemRef` (S6.1) | OPTIONAL | Definition items collected, displayed, or attested in this unit. |
| `conceptRefs` | array of `ConceptRef` (S6.2) | OPTIONAL | Registry / Ontology concepts represented in this unit. |
| `actionRefs` | array of `ActionRef` (S6.3) | OPTIONAL | Response Actions invoked from this unit (forthcoming companion spec). |
| `applicability` | object | OPTIONAL | Per-Unit applicability override (S7). |
| `accessibility` | object | OPTIONAL | Accessibility intent (S5.3). |
| `extensions` | object | OPTIONAL | `x-`-prefixed extension data. |

A Unit with zero `itemRefs`, zero `conceptRefs`, and zero `actionRefs` is permitted but contributes nothing to coverage (S8). Such units are intended for placeholder or planning purposes.

### 5.2 The `unit.kind` Registry

`unit.kind` is a **closed, abstract, task-oriented enum**. The closure is deliberate: per concept note §6.2, units must not become layout containers, and per §9 the spec stops if "units become layout containers or required fields can disappear from generated experiences without detection."

| Value | Meaning |
|-------|---------|
| `data-entry` | The user provides or revises data. |
| `review` | The user reviews previously captured data, typically read-only. |
| `confirmation` | The user affirms accuracy or intent prior to a state transition. |
| `evidence-collection` | The user provides evidence (attachments, attestations, signatures, citations). |
| `attestation` | The user certifies a statement under accountability (signing, oath, affirmation). |
| `error-resolution` | The user resolves a validation finding or correction request. |
| `assistance` | The user receives help or works with an assister or agent. |

These values describe **task intent**. They do not bind the unit to any specific Component layout — a `data-entry` unit MAY be rendered as a card, a page, a step, a panel, a CLI prompt, or any other Component-domain pattern (concept §7.1).

**Extension:** processors MUST reject `kind` values not in this table. To carry custom semantics, authors MUST use the `extensions` property with an `x-` prefix (S12); they MUST NOT introduce new top-level `kind` values.

### 5.3 Accessibility Intent

A Unit MAY declare accessibility intent that informs (not dictates) generator and renderer choices:

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `accessibility.assistive` | boolean | OPTIONAL | Whether the unit is intended to be operable with assistive technology (default: `true`). |
| `accessibility.complexity` | string (`low`, `moderate`, `high`) | OPTIONAL | Estimated cognitive complexity. Informative; processors MAY use this to bias toward simpler widgets or to surface help references. |
| `accessibility.requiresLiteracy` | boolean | OPTIONAL | Whether the unit presumes reading fluency. Informative. |

Accessibility intent does NOT define WCAG conformance, ARIA roles, or any concrete accessibility implementation. Those live in Component / Theme renderer profiles.
```

- [ ] **Step 2: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md
git commit -m "feat(experience): S5 Units + closed unit.kind registry

Unit shape, closed kind registry (data-entry, review, confirmation,
evidence-collection, attestation, error-resolution, assistance), accessibility
intent. Closure enforces the concept-note §9 stop condition."
```

---

## Task 8: Write S6 Typed References

**Files:**
- Modify: `specs/experience/experience-spec.md`

- [ ] **Step 1: Append S6**

```markdown
## 6. Typed References

Typed references bind a Unit to specific Definition / Registry / Action identities. All references are **by identifier**; Experience does NOT inline the referenced content.

### 6.1 ItemRef

References a Definition item by canonical path.

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `path` | string | REQUIRED | Canonical Definition item path. For repeat-group children, use the unindexed path (e.g., `household.members[].firstName`). |
| `purpose` | string (`collect`, `display`, `attest`, `cite`) | OPTIONAL | The user-facing purpose for this reference within the unit. Default: `collect` for `data-entry` and `evidence-collection`; `display` for `review` and `confirmation`. |
| `description` | string | OPTIONAL | Optional clarifying note for generators and reviewers. |

The `path` MUST resolve in the loaded Definition. Resolution semantics for repeat-group items: an `itemRef.path` of `household.members[].firstName` covers every instance under `household.members[*]`. A processor MUST treat that ItemRef as covering all current and future instances of `firstName` within `household.members`.

A processor MUST report a finding for any `ItemRef.path` that does not resolve in the target Definition.

### 6.2 ConceptRef

References a Registry / Ontology concept identifier.

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | string | REQUIRED | Concept identifier (e.g., Registry concept id, ontology IRI). |
| `source` | string (`registry`, `ontology`, `external`) | OPTIONAL | Origin of the concept identifier. Default: `registry`. |
| `description` | string | OPTIONAL | Optional clarifying note. |

ConceptRefs are informative for processors that do not load a Registry or Ontology Document. A Coverage-aware processor that loads a Registry / Ontology MUST report a finding for unresolved `ConceptRef.id`.

### 6.3 ActionRef

References a Response Action identifier (forthcoming companion spec, concept §10.2).

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | string | REQUIRED | Response Action identifier. |
| `role` | string (`primary`, `secondary`, `escape`) | OPTIONAL | The action's role in this unit. Default: `primary`. |
| `description` | string | OPTIONAL | Optional clarifying note. |

Until the Response Actions companion spec lands, `ActionRef.id` is a free string. Processors MUST NOT reject an Experience because `ActionRef.id` does not resolve — resolution depends on a sibling spec that does not yet exist. Coverage-aware processors MAY emit an informative finding ("ActionRef target spec not present").
```

- [ ] **Step 2: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md
git commit -m "feat(experience): S6 Typed References

ItemRef (path-based, repeat-group coverage rule), ConceptRef (registry/ontology
delegation), ActionRef (forward-compatible with Response Actions spec)."
```

---

## Task 9: Write S7 Applicability

**Files:**
- Modify: `specs/experience/experience-spec.md`

- [ ] **Step 1: Append S7**

```markdown
## 7. Applicability

**Applicability** declares the contexts in which an Experience Document or a Unit is intended to apply. It is metadata that informs generation and selection — it does NOT alter Core processing.

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `actorRefs` | array of string | OPTIONAL | Restrict applicability to these actors. Empty / omitted → all actors. |
| `platforms` | array of string | OPTIONAL | Target platforms (e.g., `web`, `mobile`, `pdf`, `cli`, `voice`, `agent`). Open string set; not normatively enumerated. |
| `locales` | array of string (BCP 47) | OPTIONAL | Locale tags this applies to. |
| `posture` | string | OPTIONAL | Renderer or session posture (e.g., `kiosk`, `assisted`, `respondent-self-serve`). Informative; not enumerated. |
| `channels` | array of string | OPTIONAL | Channels (e.g., `in-person`, `remote`, `phone`, `paper`). |
| `extensions` | object | OPTIONAL | `x-`-prefixed extension data. |

Applicability resolution is **last-write-wins, document-then-unit**: a Unit's `applicability` overrides the document-level `applicability` for that Unit. There is no merge semantics.

A processor selecting an Experience for a given context (actor, platform, locale, posture, channel) SHOULD prefer documents and units whose applicability matches. This spec does NOT define a tie-break algorithm — that belongs to a profile or selector spec.

Applicability is INFORMATIVE for Core conformance and NORMATIVE for selectors that consume it (out of scope for this document).
```

- [ ] **Step 2: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md
git commit -m "feat(experience): S7 Applicability

Document- and unit-level applicability with last-write-wins resolution. Open
enums; selector algorithm explicitly out of scope."
```

---

## Task 10: Write S8 Coverage Expectations (load-bearing per §9 gate)

**Files:**
- Modify: `specs/experience/experience-spec.md`

- [ ] **Step 1: Append S8**

```markdown
## 8. Coverage Expectations

Coverage is the **load-bearing static predicate** that protects against the §9 stop condition of the concept note: *"required fields can disappear from generated experiences without detection."* This section is normative for Coverage-aware processors (S1.4).

### 8.1 The Coverage Predicate

Given a Definition `D` and an Experience `E` whose `targetDefinition.url` matches `D.url`:

For every Definition item `i ∈ D.items` such that:

1. `i.bind.required` is the literal boolean `true`, AND
2. `i.bind.relevant` is NOT the literal boolean `false` (a missing `relevant`, the literal `true`, or an FEL expression that is not the literal `false` all satisfy this), AND
3. `i` is not transitively inside a repeat group whose `minRepeat` is `0` (such items are *conditionally* required and excluded from static coverage),

there MUST exist at least one `Unit u ∈ E.units` and at least one `ItemRef r ∈ u.itemRefs` such that `r.path` matches `i.path` under the repeat-group rule in S6.1.

If no such Unit exists, the item is **uncovered**.

### 8.2 Coverage Findings

A Coverage-aware processor MUST emit a finding for every uncovered required visible item. Each finding MUST carry:

| Field | Type | Description |
|-------|------|-------------|
| `code` | string | `EXP-COVERAGE-UNCOVERED-REQUIRED-ITEM` |
| `severity` | string | `warning` for `Experience Coverage-Aware`; processors MAY elevate to `error` per profile. |
| `path` | string | The uncovered Definition item path. |
| `experienceId` | string | The Experience's `name` or document URI for source attribution. |
| `message` | string | Human-readable explanation. |

Coverage findings are **reportable**, not blocking. The additive invariant (S1.3) forbids this spec from blocking validation, submission, or processing of the underlying Response. Coverage findings inform generators and reviewers; they do not invalidate a Response.

### 8.3 What Coverage Does NOT Check

The static predicate is intentionally narrow. It does NOT check:

- Items whose `required` is an FEL expression (conditional requiredness). These are *best-effort* — processors MAY surface an informative finding but MUST NOT treat them as uncovered.
- Items whose `relevant` is an FEL expression that is not the literal `false`. These are conservatively treated as potentially visible (i.e., they are subject to the predicate).
- Whether the user can actually reach the unit at runtime (that is a renderer / Component / Response Actions concern).
- Whether the unit is well-formed for the user — `unit.kind`, `taskRefs`, `actorRef`, and accessibility intent are not part of coverage.

Future revisions MAY add a *dynamic coverage* predicate that exercises FEL evaluation against representative posture / Response snapshots. This spec defers that to a profile or a Trace-driven check (concept §10.6).
```

- [ ] **Step 2: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md
git commit -m "feat(experience): S8 Coverage Expectations (load-bearing)

Static coverage predicate over (Definition, Experience); reportable
EXP-COVERAGE-UNCOVERED-REQUIRED-ITEM finding; explicit scope limits to keep the
predicate decidable without FEL evaluation. Satisfies §9 stop condition from
the concept note."
```

---

## Task 11: Write S9 Seed-from-Definition (informative)

**Files:**
- Modify: `specs/experience/experience-spec.md`

- [ ] **Step 1: Append S9**

```markdown
## 9. Seeding an Experience from a Definition

*This section is informative, not normative.*

Tools MAY seed an Experience Document from a Definition. A reasonable seed strategy:

1. For each top-level Definition item or group, create a `Unit` with `kind: "data-entry"`.
2. For repeat groups, create one Unit per group whose `itemRefs[]` covers the group's children.
3. Place a single `Unit` with `kind: "confirmation"` and zero `itemRefs` at the end (the seeded experience can subsequently be edited to populate confirmation references).
4. Populate `actors` with a single `applicant` actor; assign every Unit `actorRef: "applicant"`.

This is a starting point, not a recommendation. Experience and Definition are **different concepts** (concept §6.2): a Definition group describes data structure (`household.members[]`); an Experience Unit describes task intent (`identify applicant`). Seeded Experiences SHOULD be edited to reflect actual task structure, not left as 1:1 mirrors of Definition shape.

A seeded Experience that has been edited by a human or generator MUST satisfy the same schema and coverage rules as a hand-authored Experience. Seeding does not create a privileged document class.
```

- [ ] **Step 2: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md
git commit -m "feat(experience): S9 Seed-from-Definition (informative)

Non-normative guidance for tools that bootstrap Experiences from Definitions.
Explicit warning that Definition groups != Experience units."
```

---

## Task 12: Write S10 Processing Model

**Files:**
- Modify: `specs/experience/experience-spec.md`

- [ ] **Step 1: Append S10**

```markdown
## 10. Processing Model

Experience has a minimal processing model. The four-phase Core cycle (Core S2.4) is unaffected.

An **Experience Core** processor MUST:

1. **Load.** Parse the Experience Document as JSON and validate it against the schema in S11.
2. **Resolve target.** Read `targetDefinition.url` and verify it matches the loaded Definition's `url`. If `compatibleVersions` is present, the loaded Definition's `version` MUST satisfy the semver range.
3. **Verify referential integrity.** Every `actorRef`, `taskRef`, and `unit.actorRef` / `unit.taskRefs[]` MUST resolve within the document. Unresolvable references MUST produce an `EXP-REFERENTIAL-INTEGRITY` finding.
4. **Verify item-ref resolvability.** Every `ItemRef.path` MUST resolve to an Item in the loaded Definition. Unresolvable paths MUST produce an `EXP-ITEM-REF-UNRESOLVED` finding.

An **Experience Coverage-Aware** processor MUST additionally:

5. **Compute coverage.** Apply the predicate in S8.1.
6. **Emit coverage findings.** For every uncovered required visible item, emit an `EXP-COVERAGE-UNCOVERED-REQUIRED-ITEM` finding (S8.2).

There is no Experience evaluation pipeline. The document is metadata; processors read it, validate it, and consult it for generation, review, and coverage. They do not "evaluate" it in the Core sense.
```

- [ ] **Step 2: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md
git commit -m "feat(experience): S10 Processing Model

Minimal load/validate/resolve/coverage cycle. Explicitly no FEL evaluation, no
Core cycle interaction."
```

---

## Task 13: Write S11 Conformance + S12 Extension Points + S13 Security

**Files:**
- Modify: `specs/experience/experience-spec.md`

- [ ] **Step 1: Append S11, S12, S13**

```markdown
## 11. Conformance

### 11.1 Conformance Levels

This specification defines two conformance levels as a strict superset:

| Level | Requirements |
|-------|--------------|
| **Experience Core** | Schema-validate, resolve target, verify referential integrity, verify item-ref resolvability. |
| **Experience Coverage-Aware** | All of Core, plus compute and report the coverage predicate (S8). |

#### 11.1.1 Experience Core

A conformant **Experience Core** processor MUST:

1. Parse and validate any Experience Document that conforms to the schema in S11.2 without error.
2. Resolve `targetDefinition.url` against a loaded Definition and verify the URL match.
3. Verify that the loaded Definition's `version` satisfies `targetDefinition.compatibleVersions` if present.
4. Resolve every `actorRef`, `taskRef`, and `unit.actorRef` / `unit.taskRefs[]` within the document.
5. Resolve every `ItemRef.path` against the loaded Definition.
6. Emit findings (`EXP-REFERENTIAL-INTEGRITY`, `EXP-ITEM-REF-UNRESOLVED`) for unresolved references; processors MUST NOT silently drop unresolved references.

#### 11.1.2 Experience Coverage-Aware

A conformant **Experience Coverage-Aware** processor MUST:

1. Satisfy all Experience Core requirements.
2. Compute the coverage predicate of S8.1 for every loaded (Definition, Experience) pair.
3. Emit an `EXP-COVERAGE-UNCOVERED-REQUIRED-ITEM` finding for every uncovered required visible item.
4. NOT block validation, submission, or any Core operation on the basis of coverage findings. Coverage is reportable, not blocking.

### 11.2 Schema

<!-- schema-ref:start id=experience-top-level schema=schemas/experience.schema.json pointers=# -->
<!-- schema-ref:end -->

### 11.3 `$defs` Reference

<!-- schema-ref:start id=experience-defs schema=schemas/experience.schema.json pointers=#/$defs/Actor,#/$defs/Task,#/$defs/Unit,#/$defs/UnitKind,#/$defs/ItemRef,#/$defs/ConceptRef,#/$defs/ActionRef,#/$defs/Applicability,#/$defs/Accessibility,#/$defs/TargetDefinition -->
<!-- schema-ref:end -->

### 11.4 Conformance Prohibitions

A conformant processor MUST NOT:

1. Use Experience to alter Core data capture, validation, requiredness, relevance, calculation, or any other Core semantics.
2. Substitute Experience for missing Definition behavior — Experience is metadata, not a fallback.
3. Block Response submission, draft persistence, or any Core operation on the basis of an Experience finding.
4. Add `unit.kind` values outside the registry in S5.2 without using the `x-` extension mechanism.

## 12. Extension Points

Experience supports the standard Formspec extension model. Authors MAY add custom data via `x-`-prefixed properties at the document level (`extensions`), and within `Actor`, `Task`, `Unit`, `ItemRef`, `ConceptRef`, `ActionRef`, and `Applicability` objects.

Extensions MUST NOT:

1. Override or alter any property defined by this specification.
2. Introduce a parallel `kind` taxonomy under a different property name.
3. Carry behavior that would block Core operations.

Common extension patterns (informative):

- `x-figmaNode` — a link to a Figma frame anchoring the unit's design.
- `x-jiraIssue` — a tracking-system identifier for the work that produced this unit.
- `x-author` — a structured author identifier for editorial provenance.

## 13. Security Considerations

Experience Documents carry no respondent data, no PII, and no credentials. The primary security considerations are:

- **Untrusted document loading.** Experience Documents loaded from external sources MUST be validated against the schema before any processor consumes them. A maliciously crafted Experience cannot affect Core processing (additive invariant), but it MAY exhaust resources via large `units[]` or `itemRefs[]` arrays. Processors SHOULD impose reasonable size limits.
- **URI resolution.** `targetDefinition.url` and any `conceptRef.id` containing a URL MUST NOT be blindly fetched. Maintain an allowlist of Definition / Registry / Ontology sources.
- **Information disclosure.** Experience metadata MAY reveal data model structure to an attacker (Definition paths, concept identifiers, action identifiers). Treat Experience Documents as sensitive at the same level as the underlying Definition.
- **Extension content.** `x-`-prefixed extension data is untrusted; processors that render extension content (e.g., for review tooling) MUST sanitize it.

There is no prompt-injection surface in this spec — Experience does not interact with Assist providers directly. Where downstream tools (Trace, Studio regeneration review) consume Experience for LLM-mediated review, those tools own their prompt-injection mitigations.

---

## Appendix A: Full Example — Grant Application

*Populated in Task 23.*

---

## Appendix B: References

| Tag | Reference |
|---|---|
| [rfc2119] | Bradner, S., "Key words for use in RFCs to Indicate Requirement Levels", BCP 14, RFC 2119, March 1997. |
| [RFC 8174] | Leiba, B., "Ambiguity of Uppercase vs Lowercase in RFC 2119 Key Words", BCP 14, RFC 8174, May 2017. |
| [RFC 8259] | Bray, T., Ed., "The JavaScript Object Notation (JSON) Data Interchange Format", STD 90, RFC 8259, December 2017. |
| [RFC 3986] | Berners-Lee, T., Fielding, R., and L. Masinter, "Uniform Resource Identifier (URI): Generic Syntax", STD 66, RFC 3986, January 2005. |
| [RFC 6901] | Bryan, P., Ed., Zyp, K., and M. Nottingham, Ed., "JavaScript Object Notation (JSON) Pointer", RFC 6901, April 2013. |
| Concept | Formspec Semantic Layers (Experience / Response Actions / Trace), thoughts/specs/2026-05-20-formspec-semantic-layers.md. |

*End of Formspec Experience Specification.*
```

- [ ] **Step 2: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md
git commit -m "feat(experience): S11 Conformance + S12 Extensions + S13 Security

Two conformance levels (Core / Coverage-Aware), schema-ref markers awaiting
schema, extension model, security considerations. Appendix A is a stub awaiting
the worked example."
```

---

## Task 14: Create schema envelope + failing schema-acceptance test (TDD red)

**Files:**
- Create: `tests/conformance/schemas/test_experience_schema.py`
- Create: `tests/conformance/fixtures/experience/valid-minimal.json`
- Create: `schemas/experience.schema.json` (envelope only)

- [ ] **Step 1: Write failing schema-acceptance test FIRST**

Create `tests/conformance/schemas/test_experience_schema.py`:

```python
"""Schema acceptance tests for the Experience companion spec.

Loads schemas/experience.schema.json and validates fixtures under
tests/conformance/fixtures/experience/.
"""
import json
from pathlib import Path

import jsonschema
import pytest

SCHEMA_PATH = Path(__file__).resolve().parents[3] / "schemas" / "experience.schema.json"
FIXTURES_DIR = Path(__file__).resolve().parents[2] / "conformance" / "fixtures" / "experience"


@pytest.fixture(scope="module")
def schema():
    with SCHEMA_PATH.open() as f:
        return json.load(f)


@pytest.fixture(scope="module")
def validator(schema):
    return jsonschema.Draft202012Validator(schema)


def _load(name: str) -> dict:
    with (FIXTURES_DIR / name).open() as f:
        return json.load(f)


class TestExperienceSchemaValid:
    def test_valid_minimal_passes(self, validator):
        doc = _load("valid-minimal.json")
        errors = list(validator.iter_errors(doc))
        assert errors == [], f"Expected no errors, got: {[e.message for e in errors]}"


class TestExperienceSchemaInvalid:
    def test_missing_target_definition_rejected(self, validator):
        doc = _load("invalid-missing-target.json")
        errors = list(validator.iter_errors(doc))
        assert any("targetDefinition" in e.message for e in errors), errors

    def test_bad_unit_kind_rejected(self, validator):
        doc = _load("invalid-bad-unit-kind.json")
        errors = list(validator.iter_errors(doc))
        assert any("kind" in e.message or "enum" in e.message for e in errors), errors
```

- [ ] **Step 2: Create the smallest valid fixture**

Create `tests/conformance/fixtures/experience/valid-minimal.json`:

```json
{
  "$formspecExperience": "1.0",
  "version": "1.0.0",
  "targetDefinition": {
    "url": "https://example.gov/forms/intake",
    "compatibleVersions": ">=1.0.0 <2.0.0"
  }
}
```

- [ ] **Step 3: Create empty schema, run test, verify it fails**

Create `schemas/experience.schema.json` with bare envelope:

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://formspec.org/schemas/experience/1.0",
  "title": "Formspec Experience",
  "description": "Placeholder envelope.",
  "type": "object"
}
```

Run: `cd formspec && python3 -m pytest tests/conformance/schemas/test_experience_schema.py -v`
Expected: `test_valid_minimal_passes` PASSES (no constraints yet) but the two `Invalid` tests FAIL because the placeholder schema accepts everything. This is the failing-test red state for Tasks 15+.

- [ ] **Step 4: Write the full envelope**

Replace `schemas/experience.schema.json`:

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://formspec.org/schemas/experience/1.0",
  "title": "Formspec Experience",
  "description": "A Formspec Experience Document per the Experience specification. A standalone sidecar that names abstract task intent for a Formspec Definition: actors, tasks, units, applicability, and typed references to items, concepts, and actions. Like Theme, Component, Locale, and References documents, an Experience Document targets a Definition but lives alongside it. Multiple Experience Documents MAY target the same Definition (e.g., different actor populations, platforms, or postures). Experience metadata MUST NOT alter core behavioral semantics (required, relevant, readonly, calculate, validation).",
  "type": "object",
  "required": ["$formspecExperience", "version", "targetDefinition"],
  "additionalProperties": false,
  "patternProperties": {
    "^x-": {}
  },
  "properties": {
    "$formspecExperience": {
      "type": "string",
      "const": "1.0",
      "description": "Experience specification version. MUST be '1.0'.",
      "examples": ["1.0"],
      "x-lm": {
        "critical": true,
        "intent": "Version pin for experience document compatibility"
      }
    },
    "version": {
      "type": "string",
      "minLength": 1,
      "description": "Version of this Experience Document. SemVer is RECOMMENDED.",
      "examples": ["1.0.0"],
      "x-lm": {
        "critical": true,
        "intent": "Experience document revision identifier"
      }
    },
    "targetDefinition": {
      "$ref": "https://formspec.org/schemas/component/1.0#/$defs/TargetDefinition",
      "description": "Binding to the target Definition document. Same shape as Theme / Component / Locale.",
      "x-lm": {
        "critical": true,
        "intent": "Target Definition URL and compatible-versions range"
      }
    },
    "name": { "type": "string", "pattern": "^[a-zA-Z][a-zA-Z0-9_\\-]*$" },
    "title": { "type": "string" },
    "description": { "type": "string" },
    "applicability": { "$ref": "#/$defs/Applicability" },
    "actors": {
      "type": "array",
      "items": { "$ref": "#/$defs/Actor" },
      "uniqueItems": true
    },
    "tasks": {
      "type": "array",
      "items": { "$ref": "#/$defs/Task" },
      "uniqueItems": true
    },
    "units": {
      "type": "array",
      "items": { "$ref": "#/$defs/Unit" },
      "x-lm": {
        "critical": true,
        "intent": "Substantive payload; units organize typed references under tasks"
      }
    },
    "extensions": {
      "type": "object",
      "propertyNames": { "pattern": "^x-" }
    }
  },
  "$defs": {}
}
```

- [ ] **Step 5: Run test; expect minimal-valid PASS and the invalid tests still failing (because invalid fixtures don't exist yet)**

Run: `cd formspec && python3 -m pytest tests/conformance/schemas/test_experience_schema.py::TestExperienceSchemaValid -v`
Expected: `test_valid_minimal_passes` PASSES.

Run: `cd formspec && python3 -m pytest tests/conformance/schemas/test_experience_schema.py::TestExperienceSchemaInvalid -v`
Expected: Both tests FAIL with `FileNotFoundError` — invalid fixtures are added in Task 18.

- [ ] **Step 6: Commit**

```bash
cd formspec && git add schemas/experience.schema.json tests/conformance/schemas/test_experience_schema.py tests/conformance/fixtures/experience/valid-minimal.json
git commit -m "feat(experience): schema envelope + minimal-valid fixture (TDD red)

Schema envelope, doctype marker, target-definition cross-ref to Component's
canonical type. Schema acceptance test in place; \$defs intentionally empty —
populated in Tasks 15-19."
```

---

## Task 15: Schema $defs — Actor, Task, TargetDefinition fallback

**Files:**
- Modify: `schemas/experience.schema.json`

- [ ] **Step 1: Verify the Component schema already defines TargetDefinition**

Run: `grep -n '"TargetDefinition"' formspec/schemas/component.schema.json | head -3`
Expected: at least one match under `$defs`. If absent, the spec falls back to defining `TargetDefinition` locally — but per `schema-authoring-guide.md` S4.3 the Component schema owns the canonical type.

- [ ] **Step 2: Add Actor + Task to `$defs`**

Edit `schemas/experience.schema.json`, replacing `"$defs": {}` with:

```json
"$defs": {
  "Actor": {
    "type": "object",
    "required": ["id"],
    "additionalProperties": false,
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-zA-Z][a-zA-Z0-9_]*$",
        "description": "Stable identifier for this Actor. Unique within actors[].",
        "examples": ["applicant", "reviewer"]
      },
      "title": { "type": "string" },
      "description": { "type": "string" },
      "extensions": {
        "type": "object",
        "propertyNames": { "pattern": "^x-" }
      }
    }
  },
  "Task": {
    "type": "object",
    "required": ["id"],
    "additionalProperties": false,
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-zA-Z][a-zA-Z0-9_]*$",
        "description": "Stable identifier for this Task. Unique within tasks[].",
        "examples": ["identifyApplicant", "reviewEligibility"]
      },
      "title": { "type": "string" },
      "description": { "type": "string" },
      "actorRefs": {
        "type": "array",
        "items": { "type": "string", "pattern": "^[a-zA-Z][a-zA-Z0-9_]*$" }
      },
      "extensions": {
        "type": "object",
        "propertyNames": { "pattern": "^x-" }
      }
    }
  }
}
```

- [ ] **Step 3: Run the schema-acceptance test; minimal-valid should still pass**

Run: `cd formspec && python3 -m pytest tests/conformance/schemas/test_experience_schema.py::TestExperienceSchemaValid -v`
Expected: PASSES.

- [ ] **Step 4: Commit**

```bash
cd formspec && git add schemas/experience.schema.json
git commit -m "feat(experience): schema \$defs Actor + Task"
```

---

## Task 16: Schema $defs — Unit + UnitKind closed enum

**Files:**
- Modify: `schemas/experience.schema.json`

- [ ] **Step 1: Add UnitKind and Unit to `$defs`**

Append inside `$defs` (after `Task`):

```json
"UnitKind": {
  "type": "string",
  "enum": [
    "data-entry",
    "review",
    "confirmation",
    "evidence-collection",
    "attestation",
    "error-resolution",
    "assistance"
  ],
  "description": "Closed, abstract, task-oriented unit kind. data-entry: user provides or revises data. review: read-only display of captured data. confirmation: user affirms accuracy before a transition. evidence-collection: user supplies evidence (attachments, attestations). attestation: user certifies a statement under accountability. error-resolution: user resolves a validation finding. assistance: user receives help.",
  "examples": ["data-entry", "review", "confirmation"],
  "x-lm": {
    "critical": true,
    "intent": "Closed task-oriented enum; closure prevents layout-container drift"
  }
},
"Unit": {
  "type": "object",
  "required": ["id", "kind"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-zA-Z][a-zA-Z0-9_]*$",
      "description": "Stable identifier for this Unit. Unique within units[]. Referenced by Component nodes via unitRef (forthcoming).",
      "examples": ["identity", "householdReview", "submitConfirmation"]
    },
    "kind": {
      "$ref": "#/$defs/UnitKind"
    },
    "title": { "type": "string" },
    "description": { "type": "string" },
    "actorRef": {
      "type": "string",
      "pattern": "^[a-zA-Z][a-zA-Z0-9_]*$",
      "description": "Actor this unit is intended for. MUST resolve to actors[].id."
    },
    "taskRefs": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-zA-Z][a-zA-Z0-9_]*$" },
      "description": "Tasks this unit advances. Each MUST resolve to tasks[].id."
    },
    "itemRefs": {
      "type": "array",
      "items": { "$ref": "#/$defs/ItemRef" }
    },
    "conceptRefs": {
      "type": "array",
      "items": { "$ref": "#/$defs/ConceptRef" }
    },
    "actionRefs": {
      "type": "array",
      "items": { "$ref": "#/$defs/ActionRef" }
    },
    "applicability": { "$ref": "#/$defs/Applicability" },
    "accessibility": { "$ref": "#/$defs/Accessibility" },
    "extensions": {
      "type": "object",
      "propertyNames": { "pattern": "^x-" }
    }
  }
}
```

- [ ] **Step 2: Run the schema-acceptance test**

Run: `cd formspec && python3 -m pytest tests/conformance/schemas/test_experience_schema.py::TestExperienceSchemaValid -v`
Expected: PASSES.

- [ ] **Step 3: Commit**

```bash
cd formspec && git add schemas/experience.schema.json
git commit -m "feat(experience): schema \$defs Unit + closed UnitKind enum

Closed kind registry enforces the §9 stop condition: units cannot become
layout containers via new kind values."
```

---

## Task 17: Schema $defs — ItemRef, ConceptRef, ActionRef

**Files:**
- Modify: `schemas/experience.schema.json`

- [ ] **Step 1: Append three $defs**

Append inside `$defs`:

```json
"ItemRef": {
  "type": "object",
  "required": ["path"],
  "additionalProperties": false,
  "properties": {
    "path": {
      "type": "string",
      "minLength": 1,
      "description": "Canonical Definition item path. For repeat-group children use the unindexed path (e.g., household.members[].firstName).",
      "examples": ["applicantName", "household.members[].firstName"]
    },
    "purpose": {
      "type": "string",
      "enum": ["collect", "display", "attest", "cite"],
      "description": "User-facing purpose. Default depends on enclosing unit.kind (S6.1)."
    },
    "description": { "type": "string" },
    "extensions": {
      "type": "object",
      "propertyNames": { "pattern": "^x-" }
    }
  }
},
"ConceptRef": {
  "type": "object",
  "required": ["id"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Registry / Ontology concept identifier."
    },
    "source": {
      "type": "string",
      "enum": ["registry", "ontology", "external"],
      "default": "registry"
    },
    "description": { "type": "string" },
    "extensions": {
      "type": "object",
      "propertyNames": { "pattern": "^x-" }
    }
  }
},
"ActionRef": {
  "type": "object",
  "required": ["id"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Response Action identifier (forthcoming companion spec)."
    },
    "role": {
      "type": "string",
      "enum": ["primary", "secondary", "escape"],
      "default": "primary"
    },
    "description": { "type": "string" },
    "extensions": {
      "type": "object",
      "propertyNames": { "pattern": "^x-" }
    }
  }
}
```

- [ ] **Step 2: Run schema-acceptance test**

Run: `cd formspec && python3 -m pytest tests/conformance/schemas/test_experience_schema.py::TestExperienceSchemaValid -v`
Expected: PASSES.

- [ ] **Step 3: Commit**

```bash
cd formspec && git add schemas/experience.schema.json
git commit -m "feat(experience): schema \$defs ItemRef + ConceptRef + ActionRef"
```

---

## Task 18: Schema $defs — Applicability + Accessibility; add invalid fixtures (TDD green)

**Files:**
- Modify: `schemas/experience.schema.json`
- Create: `tests/conformance/fixtures/experience/invalid-missing-target.json`
- Create: `tests/conformance/fixtures/experience/invalid-bad-unit-kind.json`

- [ ] **Step 1: Append Applicability and Accessibility to `$defs`**

```json
"Applicability": {
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "actorRefs": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-zA-Z][a-zA-Z0-9_]*$" }
    },
    "platforms": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Open enum — common values: web, mobile, pdf, cli, voice, agent."
    },
    "locales": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-zA-Z]{2,3}(-[A-Za-z0-9]+)*$" },
      "description": "BCP 47 locale tags."
    },
    "posture": { "type": "string" },
    "channels": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "extensions": {
      "type": "object",
      "propertyNames": { "pattern": "^x-" }
    }
  }
},
"Accessibility": {
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "assistive": { "type": "boolean", "default": true },
    "complexity": { "type": "string", "enum": ["low", "moderate", "high"] },
    "requiresLiteracy": { "type": "boolean" },
    "extensions": {
      "type": "object",
      "propertyNames": { "pattern": "^x-" }
    }
  }
}
```

- [ ] **Step 2: Create invalid fixtures**

Create `tests/conformance/fixtures/experience/invalid-missing-target.json`:

```json
{
  "$formspecExperience": "1.0",
  "version": "1.0.0"
}
```

Create `tests/conformance/fixtures/experience/invalid-bad-unit-kind.json`:

```json
{
  "$formspecExperience": "1.0",
  "version": "1.0.0",
  "targetDefinition": {
    "url": "https://example.gov/forms/intake",
    "compatibleVersions": ">=1.0.0 <2.0.0"
  },
  "units": [
    {
      "id": "badKindUnit",
      "kind": "page"
    }
  ]
}
```

- [ ] **Step 3: Run schema-acceptance suite; expect ALL three tests to pass**

Run: `cd formspec && python3 -m pytest tests/conformance/schemas/test_experience_schema.py -v`
Expected: All three tests (`test_valid_minimal_passes`, `test_missing_target_definition_rejected`, `test_bad_unit_kind_rejected`) PASS.

- [ ] **Step 4: Commit**

```bash
cd formspec && git add schemas/experience.schema.json tests/conformance/fixtures/experience/invalid-missing-target.json tests/conformance/fixtures/experience/invalid-bad-unit-kind.json
git commit -m "feat(experience): schema \$defs Applicability + Accessibility; invalid fixtures pass

Closes the TDD red/green loop for schema-level validation. Three schema tests
green: minimal-valid, missing-target-rejected, bad-unit-kind-rejected."
```

---

## Task 19: Wire prose ↔ schema (regenerate schema-ref tables)

**Files:**
- Modify: `specs/experience/experience-spec.md` (schema-ref markers inserted in Task 13 now resolve)
- Generated: `specs/experience/experience-spec.llm.md`, BLUF injection inside `experience-spec.md`

- [ ] **Step 1: Run docs:generate**

Run: `cd formspec && npm run docs:generate`
Expected: success. The BLUF marker fills with content from `experience-spec.bluf.md`. The two `schema-ref` markers in S11.2 and S11.3 fill with generated tables. `experience-spec.llm.md` is created.

- [ ] **Step 2: Spot-check the generated tables**

Run: `grep -A 3 'generated:schema-ref id=experience-top-level' formspec/specs/experience/experience-spec.md`
Expected: table row mentioning `$formspecExperience` and `targetDefinition`.

- [ ] **Step 3: Run docs:check**

Run: `cd formspec && npm run docs:check`
Expected: success. If `x-lm.critical` properties lack `description` or `examples`, the check will fail — fix them in `schemas/experience.schema.json` before continuing.

- [ ] **Step 4: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md specs/experience/experience-spec.llm.md
git commit -m "docs(experience): regenerate BLUF injection + schema-ref tables

npm run docs:generate populates the BLUF and two schema-ref markers. llm.md
artifact created. docs:check passes."
```

---

## Task 20: Appendix A — Grant Application worked example

**Files:**
- Modify: `specs/experience/experience-spec.md`
- Create: `tests/conformance/fixtures/experience/valid-grant-application.json`

- [ ] **Step 1: Write a realistic Experience document fixture**

Create `tests/conformance/fixtures/experience/valid-grant-application.json`:

```json
{
  "$formspecExperience": "1.0",
  "version": "1.0.0",
  "targetDefinition": {
    "url": "https://grants.example.gov/forms/sf-425",
    "compatibleVersions": ">=1.0.0 <2.0.0"
  },
  "name": "sf425-respondent",
  "title": "SF-425 — Federal Financial Report (Respondent Experience)",
  "description": "Experience targeting grantee respondents preparing the federal financial report.",
  "applicability": {
    "platforms": ["web", "pdf"],
    "locales": ["en-US"],
    "channels": ["remote"]
  },
  "actors": [
    { "id": "grantee", "title": "Grantee preparer" },
    { "id": "approver", "title": "Grantee approving official" }
  ],
  "tasks": [
    { "id": "identifyAward", "title": "Identify the federal award", "actorRefs": ["grantee"] },
    { "id": "reportTransactions", "title": "Report federal cash transactions", "actorRefs": ["grantee"] },
    { "id": "certifyReport", "title": "Certify the report under penalty of perjury", "actorRefs": ["approver"] }
  ],
  "units": [
    {
      "id": "awardIdentification",
      "kind": "data-entry",
      "actorRef": "grantee",
      "taskRefs": ["identifyAward"],
      "itemRefs": [
        { "path": "federalAgency" },
        { "path": "awardIdentifyingNumber" },
        { "path": "recipientUEI" },
        { "path": "reportingPeriodEnd" }
      ],
      "conceptRefs": [
        { "id": "concept:UEI", "source": "registry" }
      ]
    },
    {
      "id": "transactionsEntry",
      "kind": "data-entry",
      "actorRef": "grantee",
      "taskRefs": ["reportTransactions"],
      "itemRefs": [
        { "path": "cashReceipts" },
        { "path": "cashDisbursements" },
        { "path": "cashOnHand" }
      ]
    },
    {
      "id": "transactionsReview",
      "kind": "review",
      "actorRef": "grantee",
      "taskRefs": ["reportTransactions"],
      "itemRefs": [
        { "path": "cashReceipts", "purpose": "display" },
        { "path": "cashDisbursements", "purpose": "display" },
        { "path": "cashOnHand", "purpose": "display" }
      ]
    },
    {
      "id": "certification",
      "kind": "attestation",
      "actorRef": "approver",
      "taskRefs": ["certifyReport"],
      "itemRefs": [
        { "path": "certifyingOfficialName", "purpose": "attest" },
        { "path": "certifyingOfficialTitle", "purpose": "attest" },
        { "path": "certificationDate", "purpose": "attest" }
      ],
      "actionRefs": [
        { "id": "submitReport", "role": "primary" }
      ],
      "accessibility": {
        "complexity": "high",
        "requiresLiteracy": true
      }
    }
  ]
}
```

- [ ] **Step 2: Add Appendix A to the spec, referencing the fixture**

Edit `specs/experience/experience-spec.md`, replacing the `Appendix A` stub with:

```markdown
## Appendix A: Full Example — Grant Application

The following Experience targets a hypothetical SF-425 Federal Financial Report Definition. It demonstrates:

- Two actor roles (`grantee`, `approver`) with task-level actor scoping.
- A `data-entry` → `review` → `attestation` task flow expressed as three units against the same Definition items.
- `purpose` overrides on `ItemRef` (`collect` default → `display` / `attest`).
- A `conceptRef` to a Registry concept (UEI).
- An `actionRef` forward-referencing the Response Actions companion spec (`submitReport`).
- Accessibility intent (`complexity: high`, `requiresLiteracy: true`) on the certification unit.

A schema-validated copy lives at `tests/conformance/fixtures/experience/valid-grant-application.json`.

```json
{
  "$formspecExperience": "1.0",
  "version": "1.0.0",
  "targetDefinition": {
    "url": "https://grants.example.gov/forms/sf-425",
    "compatibleVersions": ">=1.0.0 <2.0.0"
  },
  "name": "sf425-respondent",
  "title": "SF-425 — Federal Financial Report (Respondent Experience)",
  "actors": [
    { "id": "grantee", "title": "Grantee preparer" },
    { "id": "approver", "title": "Grantee approving official" }
  ],
  "tasks": [
    { "id": "identifyAward", "actorRefs": ["grantee"] },
    { "id": "reportTransactions", "actorRefs": ["grantee"] },
    { "id": "certifyReport", "actorRefs": ["approver"] }
  ],
  "units": [
    {
      "id": "awardIdentification",
      "kind": "data-entry",
      "actorRef": "grantee",
      "taskRefs": ["identifyAward"],
      "itemRefs": [
        { "path": "federalAgency" },
        { "path": "awardIdentifyingNumber" },
        { "path": "recipientUEI" }
      ]
    },
    {
      "id": "certification",
      "kind": "attestation",
      "actorRef": "approver",
      "taskRefs": ["certifyReport"],
      "itemRefs": [
        { "path": "certifyingOfficialName", "purpose": "attest" }
      ],
      "actionRefs": [{ "id": "submitReport", "role": "primary" }],
      "accessibility": { "complexity": "high" }
    }
  ]
}
```

(Truncated for inline readability; the full fixture lives in the test corpus.)
```

- [ ] **Step 3: Verify the fixture validates**

Add this test method to `tests/conformance/schemas/test_experience_schema.py` `TestExperienceSchemaValid` class:

```python
    def test_valid_grant_application_passes(self, validator):
        doc = _load("valid-grant-application.json")
        errors = list(validator.iter_errors(doc))
        assert errors == [], f"Expected no errors, got: {[e.message for e in errors]}"
```

Run: `cd formspec && python3 -m pytest tests/conformance/schemas/test_experience_schema.py -v`
Expected: 4 tests, all PASS.

- [ ] **Step 4: Commit**

```bash
cd formspec && git add specs/experience/experience-spec.md tests/conformance/fixtures/experience/valid-grant-application.json tests/conformance/schemas/test_experience_schema.py
git commit -m "feat(experience): Appendix A worked example (SF-425 grant application)

Realistic Experience document exercising actor scoping, three unit kinds
(data-entry/review/attestation), conceptRef, actionRef, accessibility intent,
purpose overrides. Schema-validated fixture; truncated inline example in
Appendix A."
```

---

## Task 21: Coverage predicate test + fixtures (TDD red → green)

**Files:**
- Create: `tests/conformance/spec/test_experience_coverage.py`
- Create: `tests/conformance/fixtures/experience/definition-base.json`
- Create: `tests/conformance/fixtures/experience/coverage-pass.json`
- Create: `tests/conformance/fixtures/experience/coverage-fail.json`

- [ ] **Step 1: Write the failing coverage test FIRST**

Create `tests/conformance/spec/test_experience_coverage.py`:

```python
"""Coverage predicate tests for the Experience companion spec.

Implements the static coverage predicate of Experience S8.1 in Python and
verifies it against PASS / FAIL fixtures.

This is the reference Python implementation; runtime implementations in Rust
(formspec-eval) and TS (formspec-engine) MAY arrive in follow-on tickets. The
fixtures pin behavior across implementations.
"""
import json
from pathlib import Path

import pytest

FIXTURES_DIR = Path(__file__).resolve().parents[2] / "conformance" / "fixtures" / "experience"


def _load(name: str) -> dict:
    with (FIXTURES_DIR / name).open() as f:
        return json.load(f)


def _required_visible_paths(definition: dict) -> set[str]:
    """Return paths of Definition items that are statically required and not
    statically non-relevant (Experience S8.1)."""
    paths: set[str] = set()
    for item in definition.get("items", []):
        bind = item.get("bind", {})
        required = bind.get("required")
        relevant = bind.get("relevant")
        if required is True and relevant is not False:
            paths.add(item["path"])
    return paths


def _covered_paths(experience: dict) -> set[str]:
    paths: set[str] = set()
    for unit in experience.get("units", []):
        for ref in unit.get("itemRefs", []):
            paths.add(ref["path"])
    return paths


def coverage_findings(definition: dict, experience: dict) -> list[dict]:
    """Compute EXP-COVERAGE-UNCOVERED-REQUIRED-ITEM findings per S8.2."""
    required = _required_visible_paths(definition)
    covered = _covered_paths(experience)
    uncovered = required - covered
    return [
        {
            "code": "EXP-COVERAGE-UNCOVERED-REQUIRED-ITEM",
            "severity": "warning",
            "path": p,
            "experienceId": experience.get("name", experience.get("title", "")),
            "message": f"Required visible item '{p}' is not referenced by any unit.itemRefs.",
        }
        for p in sorted(uncovered)
    ]


@pytest.fixture(scope="module")
def definition_base():
    return _load("definition-base.json")


class TestExperienceCoverage:
    def test_coverage_pass_yields_no_findings(self, definition_base):
        experience = _load("coverage-pass.json")
        findings = coverage_findings(definition_base, experience)
        assert findings == [], f"Expected no findings, got: {findings}"

    def test_coverage_fail_yields_finding_for_uncovered_item(self, definition_base):
        experience = _load("coverage-fail.json")
        findings = coverage_findings(definition_base, experience)
        assert len(findings) == 1, findings
        finding = findings[0]
        assert finding["code"] == "EXP-COVERAGE-UNCOVERED-REQUIRED-ITEM"
        assert finding["path"] == "applicantName"
        assert finding["severity"] == "warning"
```

- [ ] **Step 2: Create the shared Definition fixture (3 items; one required+visible, one optional, one statically non-relevant)**

Create `tests/conformance/fixtures/experience/definition-base.json`:

```json
{
  "$formspec": "1.0",
  "url": "https://example.gov/forms/coverage-demo",
  "version": "1.0.0",
  "status": "draft",
  "title": "Coverage demo definition",
  "items": [
    {
      "key": "applicantName",
      "path": "applicantName",
      "dataType": "string",
      "label": "Applicant name",
      "bind": { "required": true }
    },
    {
      "key": "middleName",
      "path": "middleName",
      "dataType": "string",
      "label": "Middle name (optional)",
      "bind": { "required": false }
    },
    {
      "key": "internalNotes",
      "path": "internalNotes",
      "dataType": "string",
      "label": "Internal notes",
      "bind": { "required": true, "relevant": false }
    }
  ]
}
```

- [ ] **Step 3: Create coverage-pass fixture (Experience that covers `applicantName`)**

Create `tests/conformance/fixtures/experience/coverage-pass.json`:

```json
{
  "$formspecExperience": "1.0",
  "version": "1.0.0",
  "name": "coverage-demo-pass",
  "targetDefinition": {
    "url": "https://example.gov/forms/coverage-demo",
    "compatibleVersions": ">=1.0.0 <2.0.0"
  },
  "units": [
    {
      "id": "identity",
      "kind": "data-entry",
      "itemRefs": [{ "path": "applicantName" }]
    }
  ]
}
```

- [ ] **Step 4: Create coverage-fail fixture (Experience that omits `applicantName`)**

Create `tests/conformance/fixtures/experience/coverage-fail.json`:

```json
{
  "$formspecExperience": "1.0",
  "version": "1.0.0",
  "name": "coverage-demo-fail",
  "targetDefinition": {
    "url": "https://example.gov/forms/coverage-demo",
    "compatibleVersions": ">=1.0.0 <2.0.0"
  },
  "units": [
    {
      "id": "noteEntry",
      "kind": "data-entry",
      "itemRefs": [{ "path": "middleName" }]
    }
  ]
}
```

- [ ] **Step 5: Run the coverage tests**

Run: `cd formspec && python3 -m pytest tests/conformance/spec/test_experience_coverage.py -v`
Expected: both tests PASS. `test_coverage_pass_yields_no_findings` verifies no findings when `applicantName` is covered. `test_coverage_fail_yields_finding_for_uncovered_item` verifies one finding with code `EXP-COVERAGE-UNCOVERED-REQUIRED-ITEM` and path `applicantName`. `internalNotes` is excluded because `relevant: false`. `middleName` is excluded because `required: false`.

- [ ] **Step 6: Commit**

```bash
cd formspec && git add tests/conformance/spec/test_experience_coverage.py tests/conformance/fixtures/experience/definition-base.json tests/conformance/fixtures/experience/coverage-pass.json tests/conformance/fixtures/experience/coverage-fail.json
git commit -m "feat(experience): coverage predicate reference impl + PASS/FAIL fixtures

Python reference for Experience S8.1. PASS fixture covers the one
required-visible item; FAIL fixture omits it and yields one finding. Optional
items and statically non-relevant items are correctly excluded from the
predicate."
```

---

## Task 22: Add dangling-item-ref invalid fixture + extend schema test

**Files:**
- Create: `tests/conformance/fixtures/experience/invalid-dangling-item-ref.json`
- Modify: `tests/conformance/spec/test_experience_coverage.py`

- [ ] **Step 1: Create the dangling-item-ref fixture**

This fixture is schema-valid (`itemRef.path` is a non-empty string) but the path does not resolve in `definition-base.json`. Resolution is a processor-level check, not a schema-level check — so we test it in `test_experience_coverage.py` rather than `test_experience_schema.py`. This honors the spec's split (S10: referential integrity is a processor concern; the schema does not encode Definition state).

Create `tests/conformance/fixtures/experience/invalid-dangling-item-ref.json`:

```json
{
  "$formspecExperience": "1.0",
  "version": "1.0.0",
  "name": "dangling-ref-fail",
  "targetDefinition": {
    "url": "https://example.gov/forms/coverage-demo",
    "compatibleVersions": ">=1.0.0 <2.0.0"
  },
  "units": [
    {
      "id": "ghost",
      "kind": "data-entry",
      "itemRefs": [
        { "path": "applicantName" },
        { "path": "nonexistentField" }
      ]
    }
  ]
}
```

- [ ] **Step 2: Add a referential-integrity check to the reference impl**

Append to `tests/conformance/spec/test_experience_coverage.py`:

```python
def unresolved_item_refs(definition: dict, experience: dict) -> list[dict]:
    """Compute EXP-ITEM-REF-UNRESOLVED findings per S10."""
    definition_paths = {item["path"] for item in definition.get("items", [])}
    findings: list[dict] = []
    for unit in experience.get("units", []):
        for ref in unit.get("itemRefs", []):
            if ref["path"] not in definition_paths:
                findings.append(
                    {
                        "code": "EXP-ITEM-REF-UNRESOLVED",
                        "severity": "warning",
                        "path": ref["path"],
                        "unitId": unit.get("id"),
                        "message": f"ItemRef.path '{ref['path']}' does not resolve in target Definition.",
                    }
                )
    return findings


class TestExperienceReferentialIntegrity:
    def test_dangling_item_ref_produces_finding(self, definition_base):
        experience = _load("invalid-dangling-item-ref.json")
        findings = unresolved_item_refs(definition_base, experience)
        assert len(findings) == 1
        assert findings[0]["code"] == "EXP-ITEM-REF-UNRESOLVED"
        assert findings[0]["path"] == "nonexistentField"
        assert findings[0]["unitId"] == "ghost"

    def test_clean_experience_produces_no_finding(self, definition_base):
        experience = _load("coverage-pass.json")
        findings = unresolved_item_refs(definition_base, experience)
        assert findings == []
```

- [ ] **Step 3: Run the test**

Run: `cd formspec && python3 -m pytest tests/conformance/spec/test_experience_coverage.py -v`
Expected: 4 tests, all PASS.

- [ ] **Step 4: Commit**

```bash
cd formspec && git add tests/conformance/fixtures/experience/invalid-dangling-item-ref.json tests/conformance/spec/test_experience_coverage.py
git commit -m "feat(experience): EXP-ITEM-REF-UNRESOLVED predicate + fixture

Referential integrity is a processor concern (S10), not a schema concern.
Python reference impl + PASS/FAIL fixtures pin the finding shape."
```

---

## Task 23: Full verification — docs:generate, docs:check, types:generate, pytest

**Files:**
- Modified by tooling: `specs/experience/experience-spec.llm.md`, `packages/formspec-types/src/generated/*`

- [ ] **Step 1: Regenerate docs**

Run: `cd formspec && npm run docs:generate`
Expected: success. No diffs except possibly generated artifacts.

- [ ] **Step 2: Run docs:check**

Run: `cd formspec && npm run docs:check`
Expected: success. If failure, common causes:
- Missing `examples` on a `x-lm.critical: true` property → add to schema.
- Generated artifact stale → re-run `docs:generate`.
- Cross-spec contract violation → inspect `run-spec-contract-tests.mjs` output.

- [ ] **Step 3: Regenerate auto-derived types**

Run: `cd formspec && npm run --workspace @formspec-org/types build`
Expected: success. `packages/formspec-types/src/generated/` MAY contain new `Experience*` types; commit them if so.

- [ ] **Step 4: Run the full Python conformance suite for the new files**

Run: `cd formspec && python3 -m pytest tests/conformance/schemas/test_experience_schema.py tests/conformance/spec/test_experience_coverage.py -v`
Expected: all tests PASS (4 schema + 4 coverage = 8 tests).

- [ ] **Step 5: Run the dep-fence and filemap checks**

Run: `cd formspec && npm run docs:filemap && npm run check:deps`
Expected: both succeed. `filemap.json` may have new entries for `specs/experience/*` and `schemas/experience.schema.json`; commit them.

- [ ] **Step 6: Commit any tooling-generated artifacts**

```bash
cd formspec && git add -A
git status   # inspect what's pending; ensure no unintended changes
git commit -m "chore(experience): regenerate llm.md, types, filemap

Generated artifacts only; no hand edits."
```

---

## Task 24: Update concept-note §10.1 with forward link to landed spec

**Files:**
- Modify: `thoughts/specs/2026-05-20-formspec-semantic-layers.md`

- [ ] **Step 1: Add a one-line forward link under §10.1**

Edit `formspec/thoughts/specs/2026-05-20-formspec-semantic-layers.md`. Locate the §10.1 line:

```
1. **Experience companion spec.** Define actor, task, unit, applicability, typed references, the `unit.kind` registry, coverage expectations, seed-from-Definition guidance, and the minimum authoring bundle.
```

Replace with:

```
1. **Experience companion spec.** Define actor, task, unit, applicability, typed references, the `unit.kind` registry, coverage expectations, seed-from-Definition guidance, and the minimum authoring bundle. **Landed:** [`specs/experience/experience-spec.md`](../../specs/experience/experience-spec.md) (draft, 2026-05-21).
```

- [ ] **Step 2: Commit**

```bash
cd formspec && git add thoughts/specs/2026-05-20-formspec-semantic-layers.md
git commit -m "docs(concept-note): forward-link §10.1 to landed Experience spec"
```

---

## Task 25: Final sweep — run full formspec test suite, then push

**Files:** none

- [ ] **Step 1: Run the full formspec build + test gate**

Run: `cd formspec && npm run build && npm run docs:check && python3 -m pytest tests/conformance -v`
Expected: green. If failure, investigate before pushing.

- [ ] **Step 2: Inspect commit history**

Run: `cd formspec && git log --oneline -30`
Expected: linear history of 20-ish atomic commits, each describing a single slice.

- [ ] **Step 3: Push**

Confirm with the user that the work is ready to push, then:

```bash
cd formspec && git push
```

- [ ] **Step 4: Bump parent submodule pointer**

```bash
cd .. && git add formspec && git status
git commit -m "chore(submodules): bump formspec — land Experience companion spec draft

First follow-on from thoughts/specs/2026-05-20-formspec-semantic-layers.md §10.1.
Adds specs/experience/, schemas/experience.schema.json, and the conformance
fixtures for schema acceptance and coverage predicate."
```

- [ ] **Step 5: Confirm with user before pushing the parent**

Per stack CLAUDE.md "Submodule discipline": parent points at submodule SHAs that MUST exist remotely. Verify `git push` in Step 3 succeeded before this commit; then `git push` parent only with explicit user approval.

---

## Self-Review Pass

After completing all tasks, verify:

1. **Concept-note §6.2 coverage** — Actors, Tasks, Units, typed refs, applicability, accessibility intent, unit.kind registry, seed-from-Definition guidance: all written in Tasks 3–11. ✓
2. **§9 Experience-shape promotion gate** — abstract `unit.kind` (closed registry, Task 7), coverage expectations (S8 / coverage predicate, Task 10), seed-from-Definition guidance (S9, Task 11). ✓
3. **§10.1 follow-on order** — Experience first; landed; forward link in Task 24. ✓
4. **Additive invariant** — stated in S1.3, S1.4.1, S11.4; coverage findings are reportable not blocking (S8.2 + S11.1.2). ✓
5. **No scope creep** — Component reference additions (`unitRef`, `taskRefs`, `actionRef`), Response Actions schema, Validation mapping, Trace: all explicitly out of scope. ✓
6. **Conformance bar** — prose + schema + examples + conformance fixtures, sequenced. ✓
7. **Style-guide alignment** — frontmatter, BCP-14, BLUF, schema-ref markers, conformance levels, security considerations, references appendix. ✓
8. **Schema-guide alignment** — `$id`, `const` marker, `x-lm.critical` + examples, `additionalProperties: false`, PascalCase `$defs`, cross-schema `$ref` to canonical TargetDefinition. ✓

---

## Original Execution Handoff

Original pre-execution handoff follows. Two execution options:

**1. Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration. Good fit because each task is self-contained and the prose / schema / fixtures phases have clear handoff points.

**2. Inline Execution** — Execute tasks in this session using executing-plans, batch execution with checkpoints. Good fit if you want to steer per-task decisions in real time.

Which approach?
