{
  "fixture": "invalid-bad-version",
  "purpose": "App Manifest `version` is not a SemVer 2.0.0 string. Schema MUST reject.",
  "expected": { "valid": false, "errorContains": "version" },
  "bundle": {
    "$formspecBundle": "2.0",
    "version": "v1",
    "id": "https://example.gov/apps/bad-version",
    "definitions": [
      { "url": "https://example.gov/apps/bad-version/definition", "version": "1.0.0" }
    ]
  }
}
