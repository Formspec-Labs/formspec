/** @filedesc Canonical Formspec common extension registry document (bundled for Studio and tools). */
import registryDocument from './generated/formspec-common.registry.json' with { type: 'json' };

export default registryDocument;
