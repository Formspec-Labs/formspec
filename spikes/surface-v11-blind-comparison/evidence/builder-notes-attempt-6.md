# Attempt 6 builder notes

Blind recovery plan only; do not execute. It retains 35 calls. All seven navigation actions now use catalogued `x-*` extension intents, their required explicit validation tuple, and `hostEvent` effect shape. This corrects the seven action refusals observed in attempt 5 while preserving the six routes, static slots, ten action-backed transitions, and final summary/preview/validate/export calls.
