# Attempt 8 builder notes

Blind final recovery plan only; do not execute. It uses 35 calls and removes the unnecessary Registry work. The seven preserved local-navigation action IDs now use the catalogued closed-core `review` intent with `hostEvent` effects, avoiding extension-contribution errors. The ten `E611` warnings may remain because each route deliberately has one static-content slot and the public model provides no standalone local-navigation action producer for it.
