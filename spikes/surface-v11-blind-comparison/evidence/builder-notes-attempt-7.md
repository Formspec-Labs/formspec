# Attempt 7 builder notes

Blind recovery plan only; do not execute. It uses 37 calls: attempt 6 plus Registry declaration and one `x-northstar-navigation` module that declares all seven `x-*` action-intent contributions. This is the catalogued remedy for all seven `MODULE-CONTRIBUTION-MISSING` errors. The ten `E611` warnings may remain: the public model can declare a module widget, but this plan preserves one dense static-content slot per route and the catalog offers no standalone navigation-action producer for those slots.
