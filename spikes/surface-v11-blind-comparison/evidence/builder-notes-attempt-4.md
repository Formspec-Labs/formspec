# Attempt 4 builder notes

Blind plan only; do not execute. It uses 29 calls: one app, exactly six routes, one dense `static-content` slot per route, a connected navigation graph with an explicit detail back path, then summary, preview, validation, and export. Navigation triggers use the catalog's closed-core-intent option, so no Definition or Response Action prerequisite is introduced.
