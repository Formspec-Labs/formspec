# Attempt 5 builder notes

Blind recovery plan only; do not execute. It uses 35 calls: start, one required Definition declaration, seven catalogued Response Actions, six routes, six static-content slots, ten action-backed transitions, then summary, preview, validation, and export. This resolves all ten replayed AppGraph trigger diagnostics; the earlier 29-call count was incorrect (the original plan actually had 27 calls).
