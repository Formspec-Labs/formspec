# Formspec Cloud SaaS V1 — recovery attempt 3

The strict recovery requires structured registry, data-source, definition-form, and experience-unit calls in addition to the 57-call reachable replay. The public catalog exposes these as `edit_registry`, `materialize_data_sources`, `edit_definition`, `bind_slot`, and `add_experience_unit`; each navigation transition must be emitted by a declared widget action output to remove `E611`.
